---
phase: 03-positioning-mission-upload-inline-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/review/[id]/positioning/page.tsx
autonomous: true
requirements: [FLOW-01]

must_haves:
  truths:
    - "After creating a mission from the dialog on /review/[id]/positioning, the browser stays on the same URL (no router.push/replace to /positions/[missionId])."
    - "After upload/create success paths, no code path forces navigation to the mission detail page; optional user-initiated links remain secondary (D-01)."
  artifacts:
    - path: "app/review/[id]/positioning/page.tsx"
      provides: "Documented FLOW-01 invariants next to mission creation; verified no mandatory redirect to /positions/*"
  key_links:
    - from: "handleCreateMission onSuccess"
      to: "URL"
      via: "setSelectedMissionId only — no navigation to /positions"
      pattern: "setSelectedMissionId"
---

<objective>
Lock **FLOW-01** by auditing `/review/[id]/positioning` for any mandatory redirect to `/positions/[missionId]` after mission creation or upload, and documenting **D-01** / **D-02** in code so regressions are obvious.

Purpose: Research indicates the happy path already matches the decision; this plan makes that **grep-verifiable** and adds a minimal maintenance hook.

Output: Verified navigation contract + short inline documentation on `page.tsx`.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-positioning-mission-upload-inline-status/03-CONTEXT.md
@.planning/phases/03-positioning-mission-upload-inline-status/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit navigation + forbid router redirect to /positions on this page</name>
  <read_first>
    - app/review/[id]/positioning/page.tsx
    - .planning/phases/03-positioning-mission-upload-inline-status/03-CONTEXT.md (D-01, D-02)
    - components/job-description-input.tsx
  </read_first>
  <files>app/review/[id]/positioning/page.tsx</files>
  <action>
Per **D-01** (no mandatory redirect to `/positions/[missionId]`) and **D-02** (after `Créer la mission`, user stays on same URL with new mission selected):

1. Confirm `handleCreateMission` success callback only calls `setSelectedMissionId(mission.id)`, closes dialog, clears form — **no** `router.push` / `router.replace` / `redirect(`.
2. Confirm the only `router.push` on this file remains `handleStartPositioning` → `/review/${candidateId}/positioning/${data.id}` (positioning wizard — **not** FLOW-01 violation).
3. Search the same directory tree `app/review/[id]/positioning/` (excluding `[positioningId]/` if out of scope) for any `router.(push|replace)` targeting `/positions/`; if found on the **liste** page or shared imports used exclusively for mission create/upload, remove or change to optional `Link` (user gesture) per D-01.
4. Immediately **above** `handleCreateMission` (before line `const handleCreateMission = async`), add a block comment:

```text
// FLOW-01 / D-01 D-02: Ne pas rediriger vers /positions/[id] après création de mission depuis cet écran.
// Sélection locale uniquement (setSelectedMissionId). Lien « fiche mission » = action secondaire explicite utilisateur, pas parcours nominal.
```

Do **not** add new routes or features; documentation + fixes only if audit finds a violation.
  </action>
  <acceptance_criteria>
    - `rg "router\\.(push|replace)\\([^)]*/positions/" app/review/[id]/positioning/page.tsx` exits **1** (no match) in the project root.
    - `rg "setSelectedMissionId" app/review/[id]/positioning/page.tsx` exits **0** (at least one match in `onSuccess` of create mission).
    - `rg "FLOW-01 / D-01 D-02" app/review/[id]/positioning/page.tsx` exits **0** (comment present).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && ! rg -q 'router\.(push|replace)\([^)]*/positions/' "app/review/[id]/positioning/page.tsx" && rg -q "setSelectedMissionId" "app/review/[id]/positioning/page.tsx" && rg -q "FLOW-01 / D-01 D-02" "app/review/[id]/positioning/page.tsx"</automated>
  </verify>
  <done>No `router.push|replace` to `/positions/` on `page.tsx`; comment documents FLOW-01 for future edits.</done>
</task>

<task type="auto">
  <name>Task 2: Lint the positioning hub page</name>
  <read_first>
    - eslint.config.mjs
  </read_first>
  <files>app/review/[id]/positioning/page.tsx</files>
  <action>
Run `pnpm lint` from the repo root; if ESLint reports only issues in `page.tsx`, fix them. Do not change unrelated files.
  </action>
  <acceptance_criteria>
    - `pnpm lint` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && pnpm lint</automated>
  </verify>
  <done>Lint clean for the workspace (or page-local fixes applied).</done>
</task>

</tasks>

<verification>
- Automated greps in Task 1 pass.
- `pnpm lint` passes.
</verification>

<success_criteria>
FLOW-01 is enforced on the hub page: creation/upload success does not navigate to `/positions/[id]`; invariant is documented and grep-verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-positioning-mission-upload-inline-status/03-01-SUMMARY.md`
</output>
