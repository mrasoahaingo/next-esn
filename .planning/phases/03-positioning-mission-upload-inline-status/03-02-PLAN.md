---
phase: 03-positioning-mission-upload-inline-status
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - lib/utils/mission-positioning-gate.ts
  - components/positioning-mission-analysis-inline.tsx
  - app/review/[id]/positioning/page.tsx
  - lib/queries/missions.ts
autonomous: true
requirements: [FLOW-02, FLOW-03]

must_haves:
  truths:
    - "For the selected mission row, an inline block shows job-posting analysis state (in progress / done / error) aligned with server fields and Phase 2 step UI."
    - "The « Analyser le matching » button is disabled until server-side mission analysis is usable; help text + aria-describedby explain why (D-09)."
    - "Gating uses mission detail from useMission(selectedMissionId) — not createPositioning.isPending alone (D-03)."
  artifacts:
    - path: "lib/utils/mission-positioning-gate.ts"
      provides: "Pure helpers: persisted analysis check + CTA disabled reason for FLOW-03"
    - path: "components/positioning-mission-analysis-inline.tsx"
      provides: "Inline WorkflowStepList + useWorkflowStream for selected mission (D-04, D-05)"
    - path: "app/review/[id]/positioning/page.tsx"
      provides: "useMission integration, inline block, gated Button"
  key_links:
    - from: "page.tsx"
      to: "GET /api/missions/[id]"
      via: "useMission(selectedMissionId)"
      pattern: "useMission\\("
    - from: "positioning CTA Button"
      to: "mission-positioning-gate"
      via: "disabled + aria-describedby"
      pattern: "getPositioningCtaState|missionAllowsMatching"
---

<objective>
Implement **FLOW-02** (inline mission analysis status on `/review/[id]/positioning`) and **FLOW-03** (positioning CTA enabled only when mission job analysis is successful per **server-derived** state), following **D-03–D-09** and `03-UI-SPEC.md`.

Purpose: Users see analysis progress without leaving the hub; matching cannot start until `job_analysis` is ready (not stale, no blocking workflow error, no active run without persisted analysis).

Output: New gate utility, inline analysis component (reuse Phase 2 patterns), wired hub page.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-positioning-mission-upload-inline-status/03-CONTEXT.md
@.planning/phases/03-positioning-mission-upload-inline-status/03-RESEARCH.md
@.planning/phases/03-positioning-mission-upload-inline-status/03-UI-SPEC.md
@components/mission-job-analysis.tsx
@lib/workflow/compute-step-status.ts
@lib/hooks/useWorkflowStream.ts
</context>

<interfaces>
Executor should mirror **persisted analysis** detection from `MissionJobAnalysis` (`hasPersistedJobAnalysis` logic ~lines 172–177): non-null `job_analysis` with at least one of `expectedExpertiseLevel`, non-empty `executiveSummary`, or `keyPoints.length > 0`.

`useWorkflowStream` for job posting must use:
- `api: \`/api/missions/${missionId}/analyze-job\``
- `runStatus: 'extracting'`, `activeStatuses: ['extracting']`
- Same types as `MissionJobAnalysis`: `Partial&lt;JobPostingAnalysis&gt;`, `JobPostingAnalysisStreamMeta`

Inline component should call `computeJobPostingStepStates` + `WorkflowStepList` + `formatStepSummaryLine` / `getFrenchStepShortLabel('jobPosting', ...)` per **D-05**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mission detail type + pure CTA gate helpers (FLOW-03, D-03, D-08)</name>
  <read_first>
    - lib/queries/missions.ts
    - lib/types/workflow-last-error.ts
    - app/api/missions/[id]/route.ts
    - components/mission-job-analysis.tsx (hasPersistedJobAnalysis block)
  </read_first>
  <files>lib/queries/missions.ts, lib/utils/mission-positioning-gate.ts</files>
  <action>
1. Export a **`MissionDetail`** (or `MissionForPositioningHub`) type from `lib/queries/missions.ts` that includes at minimum: `id`, `job_analysis` (nullable), `job_analysis_workflow_run_id`, `job_analysis_stale`, `workflow_last_error` — matching JSON from `GET /api/missions/[id]`. Use existing `MissionWorkflowDiagnostics` where applicable.

2. Create **`lib/utils/mission-positioning-gate.ts`** exporting:
   - `hasPersistedJobAnalysis(job_analysis: unknown | null): boolean` — same content rules as `MissionJobAnalysis` (D-08).
   - `getPositioningMatchingCtaState(mission: MissionDetail | null | undefined): { disabled: boolean; helpText: string }` implementing **03-UI-SPEC** table (lines 97–104): French strings **exactly** as in the spec for the four rows (en cours, pas démarrée, succès, erreur). Derive `disabled` per **D-03** from **server fields only**: enable **only** when `hasPersistedJobAnalysis` && `!job_analysis_stale` && `!job_analysis_workflow_run_id` && `!workflow_last_error` (treat missing mission as disabled — « En attente du démarrage de l’analyse… » or equivalent per spec). If `workflow_last_error` or stream-equivalent failure: use ERR-02 style message from UI-SPEC line 119.

3. Do **not** add `POST /api/positioning` validation in this task (optional defense-in-depth deferred per RESEARCH).

Per **D-06**: no change to `useMissions` global refetch interval.
  </action>
  <acceptance_criteria>
    - `rg "export (type|interface) MissionDetail|MissionForPositioningHub" lib/queries/missions.ts` exits **0**.
    - `rg "getPositioningMatchingCtaState" lib/utils/mission-positioning-gate.ts` exits **0**.
    - `rg "Analyse terminée — vous pouvez lancer le matching" lib/utils/mission-positioning-gate.ts` exits **0** (success help copy from 03-UI-SPEC.md).
    - `pnpm exec tsc --noEmit` exits **0** (repo-wide typecheck).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && rg -q "getPositioningMatchingCtaState" lib/utils/mission-positioning-gate.ts && rg -q "Analyse terminée — vous pouvez lancer le matching" lib/utils/mission-positioning-gate.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Exported mission detail type; gate helper returns spec copy and correct disabled semantics for FLOW-03.</done>
</task>

<task type="auto">
  <name>Task 2: Inline analysis block component (FLOW-02, D-04, D-05)</name>
  <read_first>
    - components/mission-job-analysis.tsx (stream + computeJobPostingStepStates + WorkflowStepList)
    - components/workflow/WorkflowStepList.tsx
    - lib/workflow/compute-step-status.ts
    - .planning/phases/03-positioning-mission-upload-inline-status/03-UI-SPEC.md (bloc titre, spacing ml-8)
  </read_first>
  <files>components/positioning-mission-analysis-inline.tsx</files>
  <action>
Add **`components/positioning-mission-analysis-inline.tsx`** (client component) with props:

- `missionId: string`
- `mission: MissionDetail | undefined` (from parent `useMission` result)

Behavior:
1. **`useWorkflowStream`** — same configuration as `MissionJobAnalysis` (see **interfaces** above). Per RESEARCH pitfall 4 / discretion: **omit** `toast.success` on `onFinish` **or** use a no-op to avoid noisy toasts on the hub; still `invalidate` mission detail + list via `queryClient` like `MissionJobAnalysis.invalidate`.
2. Build `jobPostingRows` with **`computeJobPostingStepStates`** using `streamMeta`, `partialData`, `isStreaming`, `errorStepKey`, `persistedError: mission.workflow_last_error`, `workflowFailed`.
3. Render title **`Analyse de la fiche mission`** (03-UI-SPEC). Container: `aria-busy` when initial mission fetch loading if applicable; step list wrapper: `aria-live="polite"` per UI-SPEC.
4. Layout: align with parent **`ml-8`** column; spacing per 03-UI-SPEC (`mt-6` before block, internal padding md).
5. When showing step list, reuse **`WorkflowStepList`** + summary line like Phase 2. When mission is loading, show spinner + short FR text.

Do **not** duplicate full `MissionJobAnalysis` dialogs (explain, cancel) unless needed for ERR-02; minimum is visible error + steps per D-04.
  </action>
  <acceptance_criteria>
    - `rg "positioning-mission-analysis-inline" app/review/[id]/positioning/page.tsx` — file must be imported from page in Task 3; for Task 2 alone: `rg "useWorkflowStream" components/positioning-mission-analysis-inline.tsx` exits **0**.
    - `rg "computeJobPostingStepStates" components/positioning-mission-analysis-inline.tsx` exits **0**.
    - `rg "Analyse de la fiche mission" components/positioning-mission-analysis-inline.tsx` exits **0**.
    - `pnpm exec tsc --noEmit` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && rg -q "useWorkflowStream" components/positioning-mission-analysis-inline.tsx && rg -q "computeJobPostingStepStates" components/positioning-mission-analysis-inline.tsx && rg -q "Analyse de la fiche mission" components/positioning-mission-analysis-inline.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Reusable inline block streams job posting analysis with Phase 2 step list and FR title.</done>
</task>

<task type="auto">
  <name>Task 3: Wire hub page — useMission, inline block, CTA gating (FLOW-02, FLOW-03, D-09)</name>
  <read_first>
    - app/review/[id]/positioning/page.tsx
    - lib/utils/mission-positioning-gate.ts
    - components/positioning-mission-analysis-inline.tsx
    - .planning/phases/03-positioning-mission-upload-inline-status/03-UI-SPEC.md (CTA row, aria-describedby)
  </read_first>
  <files>app/review/[id]/positioning/page.tsx</files>
  <action>
1. Import **`useMission`** from `@/lib/queries` and call `useMission(selectedMissionId ?? '')` with `enabled: !!selectedMissionId` (or equivalent) so **only the selected row** triggers detail fetch + 3s refetch when analysis active (**D-06**).

2. Remove or replace the local **`interface Mission`** with **`MissionDetail`** for list typing if `GET /api/missions` shape is compatible; otherwise keep list type minimal but cast/detail merge as needed — goal is **no `any`** for selected mission detail.

3. In the expanded section (`isSelected`), **between** the « Fiche de poste » card and the CTA row, render **`<PositioningMissionAnalysisInline missionId={mission.id} mission={detailData} />`** (pass `data` from `useMission` when `selectedMissionId === mission.id`).

4. **CTA row:** Compute `const cta = getPositioningMatchingCtaState(detailMission)` using **detail** mission object from `useMission`, **not** list row alone. Set `disabled={cta.disabled || createPositioning.isPending}` (mutation still disables during submit per existing UX). Add **`id="positioning-matching-cta-help"`** on the help `<p>` and **`aria-describedby="positioning-matching-cta-help"`** on the `Button` when disabled (always link help for clarity per D-09).

5. Replace obsolete copy « Mission sélectionnée — lancez… » if it contradicts gating; help text must come from **`cta.helpText`** when using gate (you may keep a single `<p>`).

6. Do **not** call `submit`/`analyze` from `useEffect` on mount (**project rule**); analysis remains API-triggered from `POST /api/missions`.
  </action>
  <acceptance_criteria>
    - `rg "useMission\\(selectedMissionId" app/review/[id]/positioning/page.tsx` OR `rg "useMission\\(" app/review/[id]/positioning/page.tsx` exits **0**.
    - `rg "PositioningMissionAnalysisInline" app/review/[id]/positioning/page.tsx` exits **0**.
    - `rg "getPositioningMatchingCtaState" app/review/[id]/positioning/page.tsx` exits **0**.
    - `rg "aria-describedby" app/review/[id]/positioning/page.tsx` exits **0**.
    - `rg "disabled=\\{[^}]*cta\\.disabled" app/review/[id]/positioning/page.tsx` OR `rg "cta\\.disabled" app/review/[id]/positioning/page.tsx` exits **0**.
    - `pnpm lint` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && rg -q "useMission" "app/review/[id]/positioning/page.tsx" && rg -q "PositioningMissionAnalysisInline" "app/review/[id]/positioning/page.tsx" && rg -q "getPositioningMatchingCtaState" "app/review/[id]/positioning/page.tsx" && rg -q "aria-describedby" "app/review/[id]/positioning/page.tsx" && pnpm lint</automated>
  </verify>
  <done>Hub shows inline analysis + gated violet CTA per server state and UI-SPEC accessibility.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes.
- `pnpm lint` passes.
- Grep acceptance criteria for Tasks 1–3 pass.
</verification>

<success_criteria>
FLOW-02 and FLOW-03 satisfied: inline status for selected mission; « Analyser le matching » only when mission analysis is server-ready; French help + `aria-describedby` on disabled CTA.
</success_criteria>

<output>
After completion, create `.planning/phases/03-positioning-mission-upload-inline-status/03-02-SUMMARY.md`
</output>
