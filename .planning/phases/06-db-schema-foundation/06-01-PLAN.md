---
phase: 06-db-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260604144106_add_language_columns.sql
autonomous: true
requirements:
  - LANG-05

must_haves:
  truths:
    - "La colonne candidates.language TEXT NOT NULL DEFAULT 'fr' existe en DB"
    - "La colonne missions.language TEXT NOT NULL DEFAULT 'fr' existe en DB"
    - "La colonne organization_settings.default_language TEXT NOT NULL DEFAULT 'fr' existe en DB"
    - "Les lignes existantes dans chaque table restent inchangées (DEFAULT appliqué silencieusement)"
  artifacts:
    - path: "supabase/migrations/20260604144106_add_language_columns.sql"
      provides: "Migration idempotente ajoutant les 3 colonnes de langue"
      contains: "ADD COLUMN IF NOT EXISTS language"
  key_links:
    - from: "supabase/migrations/20260604144106_add_language_columns.sql"
      to: "organization_settings.default_language"
      via: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS"
      pattern: "default_language.*DEFAULT.*'fr'"
---

<objective>
Ajouter les colonnes de langue en base de données sur les trois tables concernées : `candidates.language`, `missions.language`, et `organization_settings.default_language`.

Purpose: Fournir la fondation DB pour LANG-05 (langue par défaut org) et les phases 7–8 (persistance de langue détectée par les workflows). Zéro changement de comportement en production — les colonnes ont un DEFAULT 'fr' et sont idempotentes.
Output: Un fichier de migration SQL créé dans `supabase/migrations/`.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer la migration SQL pour les colonnes de langue</name>
  <files>supabase/migrations/20260604144106_add_language_columns.sql</files>
  <read_first>
    - supabase/migrations/initial_schema.sql (structure table candidates)
    - supabase/migrations/20260401100600_create_missions.sql (structure table missions)
    - supabase/migrations/20260401101100_organization_settings.sql (structure table organization_settings)
    - supabase/migrations/20260401100200_add_time_tracking.sql (exemple de migration ADD COLUMN idempotente)
  </read_first>
  <action>
Créer le fichier `supabase/migrations/20260604144106_add_language_columns.sql` avec le contenu suivant (exactement) :

```sql
-- Phase 6: DB + Schema Foundation — LANG-05
-- Ajoute les colonnes de langue sur candidates, missions, et organization_settings.
-- NOT NULL DEFAULT 'fr' : les lignes existantes sont inchangées, comportement prod identique.
-- Toutes les instructions sont idempotentes (ADD COLUMN IF NOT EXISTS).

ALTER TABLE candidates
  ADD COLUMN IF NOT EXISTS language TEXT NOT NULL DEFAULT 'fr'
    CHECK (language IN ('fr', 'en'));

ALTER TABLE missions
  ADD COLUMN IF NOT EXISTS language TEXT NOT NULL DEFAULT 'fr'
    CHECK (language IN ('fr', 'en'));

ALTER TABLE organization_settings
  ADD COLUMN IF NOT EXISTS default_language TEXT NOT NULL DEFAULT 'fr'
    CHECK (default_language IN ('fr', 'en'));
```

Ne pas ajouter d'index — ces colonnes ne servent pas encore de critères de filtre dans cette phase.
Ne pas modifier les RLS existantes — les politiques org_id couvrent déjà ces tables.
  </action>
  <verify>
    <automated>grep -n "ADD COLUMN IF NOT EXISTS language" /Users/mrasoahaingo/Projects/perso/next-esn/supabase/migrations/20260604144106_add_language_columns.sql | wc -l | grep -q "2" && grep -n "default_language" /Users/mrasoahaingo/Projects/perso/next-esn/supabase/migrations/20260604144106_add_language_columns.sql | grep -q "NOT NULL DEFAULT 'fr'" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `supabase/migrations/20260604144106_add_language_columns.sql` existe
    - Le fichier contient exactement 3 instructions `ALTER TABLE` (candidates, missions, organization_settings)
    - Chaque instruction utilise `ADD COLUMN IF NOT EXISTS`
    - Chaque colonne a `NOT NULL DEFAULT 'fr'`
    - Chaque colonne a `CHECK (... IN ('fr', 'en'))`
    - La colonne sur `organization_settings` s'appelle `default_language` (pas `language`)
    - Les colonnes sur `candidates` et `missions` s'appellent `language`
    - grep "ADD COLUMN IF NOT EXISTS language" retourne 2 occurrences
    - grep "default_language" retourne 1 occurrence avec "NOT NULL DEFAULT 'fr'"
  </acceptance_criteria>
  <done>Le fichier de migration existe, est idempotent, couvre les 3 tables, et respecte la convention de nommage du projet (timestamp 14 chiffres, description snake_case).</done>
</task>

</tasks>

<verification>
- `ls supabase/migrations/20260604144106_add_language_columns.sql` — fichier présent
- `grep -c "ALTER TABLE" supabase/migrations/20260604144106_add_language_columns.sql` retourne `3`
- `grep "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260604144106_add_language_columns.sql` — 3 lignes
- `grep "NOT NULL DEFAULT 'fr'" supabase/migrations/20260604144106_add_language_columns.sql` — 3 lignes
- `grep "CHECK.*IN.*'fr'.*'en'" supabase/migrations/20260604144106_add_language_columns.sql` — 3 lignes
</verification>

<success_criteria>
La migration SQL est prête à être appliquée via `supabase db push` ou `supabase migration up`. Les trois tables recevront leurs colonnes de langue sans affecter les données existantes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-db-schema-foundation/06-01-SUMMARY.md`
</output>
