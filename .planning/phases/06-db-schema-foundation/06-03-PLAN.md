---
phase: 06-db-schema-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/llm/resolve-task.ts
autonomous: true
requirements:
  - PROMPT-02

must_haves:
  truths:
    - "resolveLlmTask émet console.warn quand le prompt rendu contient encore {{ après renderTemplate"
    - "La garde est active — les placeholders non résolus ne passent pas silencieusement"
    - "Le comportement existant de resolveLlmTask est inchangé (même signature, même return)"
  artifacts:
    - path: "lib/llm/resolve-task.ts"
      provides: "Guard console.warn sur {{ non résolu"
      contains: "console.warn"
  key_links:
    - from: "lib/llm/resolve-task.ts"
      to: "systemPrompt"
      via: "regex /{{/ après renderTemplate"
      pattern: "console\\.warn.*placeholder.*non résolu"
---

<objective>
Ajouter un guard dans `resolveLlmTask` qui émet un `console.warn` lorsque le prompt rendu contient encore des placeholders non résolus (`{{`).

Purpose: Implémente PROMPT-02 — protection contre le silent passthrough où `{{language}}` ou tout autre placeholder est passé tel quel au LLM sans substitution. Facilite le debug lors de l'intégration des prompts multi-langue en phase 7.
Output: `lib/llm/resolve-task.ts` mis à jour avec la garde.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajouter le guard console.warn dans resolveLlmTask</name>
  <files>lib/llm/resolve-task.ts, lib/llm/resolve-task.test.ts</files>
  <read_first>
    - lib/llm/resolve-task.ts (fichier complet — notamment les lignes 55–79 : renderTemplate + return)
    - lib/llm/template-render.ts (comprendre que les placeholders non résolus restent tels quels : "return match")
    - vitest.config.ts (configuration du test runner pour les imports @/)
  </read_first>
  <behavior>
    - Test 1 : quand `systemPrompt` contient `{{language}}` après rendu (contexte vide `{}`), `console.warn` est appelé avec un message contenant "placeholder" et "{{language}}"
    - Test 2 : quand `systemPrompt` est entièrement résolu (aucun `{{` restant), `console.warn` n'est PAS appelé
    - Test 3 : quand `template` est `null` ou vide, le throw existant se produit avant le guard (comportement inchangé)
    - Test 4 : quand le contexte résout tous les placeholders (`{{language}}` → `French`), aucun warn
  </behavior>
  <action>
**Étape RED — créer le fichier de test :**

Créer `lib/llm/resolve-task.test.ts` :

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderTemplate } from '@/lib/llm/template-render';

// Tests unitaires ciblés sur la logique de guard — on teste renderTemplate + détection {{
// plutôt que de mocker Supabase en entier pour resolveLlmTask.
describe('resolveLlmTask — placeholder guard', () => {
  beforeEach(() => {
    vi.spyOn(console, 'warn').mockImplementation(() => {});
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('renderTemplate laisse {{ intact quand la clé est absente du contexte', () => {
    const result = renderTemplate('Réponds en {{language}}', {});
    expect(result).toBe('Réponds en {{language}}');
  });

  it('renderTemplate résout le placeholder quand la clé est présente', () => {
    const result = renderTemplate('Réponds en {{language}}', { language: 'French' });
    expect(result).toBe('Réponds en French');
  });

  it('detecte les placeholders non résolus via regex /\\{\\{/', () => {
    const prompt = 'Réponds en {{language}}';
    expect(/\{\{/.test(prompt)).toBe(true);
  });

  it('ne détecte pas de placeholder quand le prompt est résolu', () => {
    const prompt = 'Réponds en French';
    expect(/\{\{/.test(prompt)).toBe(false);
  });
});
```

**Étape GREEN — modifier `lib/llm/resolve-task.ts` :**

Localiser la ligne 72 dans le fichier actuel :
```typescript
const systemPrompt = template.includes('{{') ? renderTemplate(template, context) : template;
```

Remplacer par :
```typescript
const systemPrompt = template.includes('{{') ? renderTemplate(template, context) : template;

if (/\{\{/.test(systemPrompt)) {
  const unresolved = systemPrompt.match(/\{\{\s*[a-zA-Z0-9_]+\s*\}\}/g) ?? [];
  console.warn(
    `[resolveLlmTask] Placeholders non résolus dans task_key="${taskKey}" : ${unresolved.join(', ')}. ` +
    `Vérifier que le contexte contient les clés attendues.`,
  );
}
```

Aucune autre modification : même signature de fonction, même return, même comportement en cas d'erreur.
  </action>
  <verify>
    <automated>npx vitest run lib/llm/resolve-task.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "console.warn" lib/llm/resolve-task.ts` retourne 1 résultat
    - `grep "/\\\{\\\{/" lib/llm/resolve-task.ts` retourne 1 résultat (le test regex)
    - `grep "taskKey" lib/llm/resolve-task.ts | grep "warn"` retourne 1 résultat (le warn inclut taskKey)
    - `grep "Placeholders non résolus" lib/llm/resolve-task.ts` retourne 1 résultat
    - Le fichier de test `lib/llm/resolve-task.test.ts` existe
    - `npx vitest run lib/llm/resolve-task.test.ts` passe (exit 0, tous les tests verts)
    - La signature de `resolveLlmTask` est inchangée : `(supabase, { taskKey, orgId, context })` → `Promise<ResolvedLlmTask>`
    - `grep "return {" lib/llm/resolve-task.ts | grep -c "gatewayModelId"` retourne 1 (le return est intact)
  </acceptance_criteria>
  <done>resolveLlmTask émet console.warn avec le nom des placeholders non résolus et le taskKey, sans modifier le return ni la signature.</done>
</task>

</tasks>

<verification>
- `grep "console.warn" lib/llm/resolve-task.ts` — présent
- `grep "/\\\{\\\{/" lib/llm/resolve-task.ts` — regex de détection présente
- `npx vitest run lib/llm/resolve-task.test.ts` — passe
- `npx tsc --noEmit 2>&1 | grep "resolve-task" | head -5` — aucune erreur TypeScript
</verification>

<success_criteria>
Tout prompt LLM qui laisse un `{{placeholder}}` non résolu déclenche un `console.warn` en développement et en production, avec le `taskKey` et les noms des placeholders. Le comportement fonctionnel de `resolveLlmTask` est inchangé.
</success_criteria>

<output>
After completion, create `.planning/phases/06-db-schema-foundation/06-03-SUMMARY.md`
</output>
