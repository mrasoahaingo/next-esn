---
phase: 06-db-schema-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schema.ts
  - templates/cv-dossier-layout.ts
autonomous: true
requirements:
  - LANG-05

must_haves:
  truths:
    - "extractionIdentitySchema contient un champ language: z.enum(['fr','en'])"
    - "Un map CV_LABELS de type Record<'fr'|'en', {...}> existe dans templates/cv-dossier-layout.ts"
    - "CV_LABELS est exporté mais non câblé à l'export PDF (phase 8)"
  artifacts:
    - path: "lib/schema.ts"
      provides: "extractionIdentitySchema avec champ language"
      contains: "language: z.enum"
    - path: "templates/cv-dossier-layout.ts"
      provides: "Map CV_LABELS bilingue"
      contains: "CV_LABELS"
  key_links:
    - from: "lib/schema.ts extractionIdentitySchema"
      to: "language field"
      via: "z.enum(['fr','en'])"
      pattern: "language.*z\\.enum"
---

<objective>
Mettre à jour les schémas Zod et le layout de template CV pour ajouter le support langue — sans câblage effectif dans les workflows ni dans la génération PDF (phases 7 et 8).

Purpose: Fournir les contrats TypeScript que les phases 7 et 8 consommeront. Zéro changement de comportement : le champ `language` a un `.default('fr')`, `CV_LABELS` est déclaré mais non encore utilisé dans le pipeline PDF.
Output: `lib/schema.ts` mis à jour + `CV_LABELS` ajouté dans `templates/cv-dossier-layout.ts`.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter language dans extractionSchema et extractionIdentitySchema</name>
  <files>lib/schema.ts</files>
  <read_first>
    - lib/schema.ts (fichier complet — voir les lignes 116–199 pour extractionSchema et extractionIdentitySchema)
  </read_first>
  <action>
Dans `lib/schema.ts`, modifier `extractionSchema` pour ajouter le champ `language` dans l'objet racine (au même niveau que `personalInfo`, `summary`, `experiences`, etc.).

Ajouter le champ juste AVANT `summary` (ligne ~146), de façon à ce qu'il apparaisse en premier dans le schéma :

```typescript
language: z
  .enum(['fr', 'en'])
  .default('fr')
  .describe(
    "Langue principale du CV détectée depuis le contenu (texte, intitulés de sections, descriptions). 'fr' si majoritairement en français, 'en' si majoritairement en anglais.",
  ),
```

Ensuite, mettre à jour `extractionIdentitySchema` (lignes ~183–186) pour inclure `language` dans le `.pick()` :

```typescript
export const extractionIdentitySchema = extractionSchema.pick({
  language: true,
  personalInfo: true,
  summary: true,
});
```

Ne pas modifier `extractionExperiencesSchema`, `extractionEducationSchema`, `extractionSkillsStrengthsSchema` — ces sous-schémas ne portent pas la langue.

Ne pas modifier les types dérivés (`ExtractedCV`, `ExtractionIdentity`) — ils sont inférés automatiquement via `z.infer<typeof ...>`.
  </action>
  <verify>
    <automated>grep -n "language.*z\.enum\|z\.enum.*fr.*en" /Users/mrasoahaingo/Projects/perso/next-esn/lib/schema.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "language: z" lib/schema.ts` retourne au moins 1 résultat dans `extractionSchema`
    - `grep "z.enum\(\['fr', 'en'\]\)" lib/schema.ts` retourne au moins 1 résultat
    - `grep ".default('fr')" lib/schema.ts` retourne au moins 1 résultat
    - `extractionIdentitySchema` contient `language: true` dans son `.pick({...})`
    - `grep "language: true" lib/schema.ts` retourne 1 résultat
    - TypeScript compile sans erreur : `npx tsc --noEmit` passe (ou au moins ne produit pas d'erreur sur lib/schema.ts)
  </acceptance_criteria>
  <done>extractionSchema a un champ language avec z.enum(['fr','en']).default('fr'), et extractionIdentitySchema inclut ce champ dans son pick.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter CV_LABELS dans templates/cv-dossier-layout.ts</name>
  <files>templates/cv-dossier-layout.ts</files>
  <read_first>
    - templates/cv-dossier-layout.ts (lignes 1–85 pour les imports et types existants)
  </read_first>
  <action>
Dans `templates/cv-dossier-layout.ts`, ajouter l'export `CV_LABELS` juste après les imports et avant la définition de `CvTemplateTheme` (vers la ligne 18).

Insérer le bloc suivant :

```typescript
/**
 * Labels de section du CV localisés par langue.
 * Utilisé par Phase 8 (PDF Wiring) pour afficher les titres dans la bonne langue.
 * Non câblé dans le pipeline de génération PDF dans cette phase.
 */
export const CV_LABELS: Record<'fr' | 'en', {
  docTitle: string;
  summary: string;
  skills: string;
  experiences: string;
  education: string;
  strengths: string;
  availability: string;
  contact: string;
}> = {
  fr: {
    docTitle: 'Dossier de compétences',
    summary: 'Profil',
    skills: 'Compétences',
    experiences: 'Expériences',
    education: 'Formation',
    strengths: 'Points forts',
    availability: 'Disponibilité',
    contact: 'Contact',
  },
  en: {
    docTitle: 'Competency Profile',
    summary: 'Profile',
    skills: 'Skills',
    experiences: 'Experience',
    education: 'Education',
    strengths: 'Key Strengths',
    availability: 'Availability',
    contact: 'Contact',
  },
};
```

Ne pas modifier les fonctions `resolveTheme`, `buildCvDossierLayout`, ni aucune autre logique existante du fichier. Ce bloc est purement additif.
  </action>
  <verify>
    <automated>grep -n "export const CV_LABELS" /Users/mrasoahaingo/Projects/perso/next-esn/templates/cv-dossier-layout.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const CV_LABELS" templates/cv-dossier-layout.ts` retourne 1 résultat
    - `grep "'fr'" templates/cv-dossier-layout.ts` retourne au moins 1 résultat dans CV_LABELS (clé de l'objet)
    - `grep "'en'" templates/cv-dossier-layout.ts` retourne au moins 1 résultat dans CV_LABELS (clé de l'objet)
    - `grep "Compétences\|Skills" templates/cv-dossier-layout.ts` retourne au moins 2 résultats (un par langue)
    - `grep "Expériences\|Experience" templates/cv-dossier-layout.ts` retourne au moins 2 résultats
    - `grep "Formation\|Education" templates/cv-dossier-layout.ts` retourne au moins 2 résultats
    - Le type du record est `Record<'fr' | 'en', {...}>` — grep "Record<'fr' | 'en'" retourne 1 résultat
    - TypeScript compile sans erreur sur ce fichier
  </acceptance_criteria>
  <done>CV_LABELS est exporté avec les labels FR et EN pour les 8 sections prévues. Aucune fonction existante n'est modifiée.</done>
</task>

</tasks>

<verification>
- `grep "language.*z\.enum\|z\.enum.*fr.*en" lib/schema.ts` — présent
- `grep "language: true" lib/schema.ts` — présent dans extractionIdentitySchema
- `grep "export const CV_LABELS" templates/cv-dossier-layout.ts` — présent
- `grep "'fr':\|'en':" templates/cv-dossier-layout.ts` — 2 clés du record
- `npx tsc --noEmit 2>&1 | grep -E "schema\.ts|cv-dossier" | head -10` — aucune erreur sur ces fichiers
</verification>

<success_criteria>
`extractionIdentitySchema` expose le champ `language` que la phase 7 utilisera pour persister la langue détectée. `CV_LABELS` fournit les libellés bilingues que la phase 8 câblera dans le pipeline PDF.
</success_criteria>

<output>
After completion, create `.planning/phases/06-db-schema-foundation/06-02-SUMMARY.md`
</output>
