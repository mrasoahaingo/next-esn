---
phase: 02-sub-step-progress-step-error-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260412_workflow_last_error.sql
  - lib/types/workflow-last-error.ts
  - lib/hooks/useWorkflowStream.ts
  - workflows/extract-cv.ts
  - workflows/analyze-job-posting.ts
  - workflows/positioning-analyze.ts
  - workflows/positioning-generate.ts
  - lib/queries/candidates.ts
  - lib/queries/missions.ts
  - lib/queries/positionings.ts
autonomous: true
requirements: [ERR-03]

must_haves:
  truths:
    - "NDJSON error lines may include optional stepKey next to error string for live clients"
    - "candidates, positionings, and missions rows can store workflow_last_error JSON for reload attribution"
    - "Successful workflow completion clears workflow_last_error on the affected row"
    - "handleWorkflowError writes both DB JSON and extended NDJSON frame"
  artifacts:
    - path: "supabase/migrations/20260412_workflow_last_error.sql"
      provides: "workflow_last_error JSONB columns"
    - path: "lib/hooks/useWorkflowStream.ts"
      provides: "Parses top-level stepKey on error chunks"
    - path: "workflows/extract-cv.ts"
      provides: "handleWorkflowError(candidateId, 'candidates', error, { stepKey? })"
  key_links:
    - from: "handleWorkflowError"
      to: "Supabase update"
      via: "workflow_last_error + status"
---

<objective>
Persist step-level workflow failure metadata and extend the NDJSON error contract so ERR-03 can be satisfied after page reload and during live streaming.

Purpose: CONTEXT D-03 requires parent-row persistence; live streams need a stable `stepKey` alongside the error message.

Output: Migration, shared `WorkflowLastError` type, updated `useWorkflowStream` parser, four workflow `handleWorkflowError` implementations + success-path clears, query types updated for new column.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sub-step-progress-step-error-attribution/02-CONTEXT.md
@.planning/phases/02-sub-step-progress-step-error-attribution/02-RESEARCH.md
@.planning/phases/02-sub-step-progress-step-error-attribution/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow_last_error JSONB migration and TypeScript type</name>
  <read_first>
    - supabase/migrations/20260318_add_workflow_run_id.sql
    - lib/schema.ts
    - .planning/phases/02-sub-step-progress-step-error-attribution/02-CONTEXT.md
  </read_first>
  <files>supabase/migrations/20260412_workflow_last_error.sql, lib/types/workflow-last-error.ts</files>
  <action>
Create `lib/types/workflow-last-error.ts` exporting:

```typescript
import { z } from 'zod';

export const workflowLastErrorSchema = z.object({
  stepKey: z.string(),
  message: z.string(),
});

export type WorkflowLastError = z.infer<typeof workflowLastErrorSchema>;
```

Create migration `supabase/migrations/20260412_workflow_last_error.sql` that:

- `ALTER TABLE candidates ADD COLUMN IF NOT EXISTS workflow_last_error JSONB;`
- `ALTER TABLE positionings ADD COLUMN IF NOT EXISTS workflow_last_error JSONB;`
- `ALTER TABLE missions ADD COLUMN IF NOT EXISTS workflow_last_error JSONB;`
- Adds a short `COMMENT ON COLUMN` in French for each explaining ERR-03 diagnostic storage.

Per D-02: single JSONB column, no new history table.
  </action>
  <acceptance_criteria>
    - `grep -q workflow_last_error supabase/migrations/20260412_workflow_last_error.sql` exits 0
    - `grep -q workflowLastErrorSchema lib/types/workflow-last-error.ts` exits 0
    - File `lib/types/workflow-last-error.ts` exports `WorkflowLastError` type
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && grep -q workflow_last_error supabase/migrations/20260412_workflow_last_error.sql && grep -q workflowLastErrorSchema lib/types/workflow-last-error.ts</automated>
  </verify>
  <done>Migration file exists with three ALTERs; Zod schema file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Extend useWorkflowStream to expose optional stepKey on stream errors</name>
  <read_first>
    - lib/hooks/useWorkflowStream.ts
    - .planning/phases/02-sub-step-progress-step-error-attribution/02-RESEARCH.md
  </read_first>
  <files>lib/hooks/useWorkflowStream.ts</files>
  <action>
In `consumeNdjsonStream`, extend parsed chunk type to:

```typescript
{ index?: number; data?: Partial<T>; meta?: M; error?: string; stepKey?: string }
```

When `chunk.error` is present, call the error callback with enough information for UI to read `stepKey`. Preferred pattern: attach to the `Error` instance as `(err as Error & { stepKey?: string }).stepKey = chunk.stepKey` **or** add a parallel ref/setState `lastErrorStepKey` on the hook return type `UseWorkflowStreamReturn`:

```typescript
errorStepKey: string | null;
```

Choose one approach and document in a one-line comment in French. If using return field `errorStepKey`, set it whenever an error chunk arrives; clear it on `submit` / reconnect start like `error`.

Export type so positioning pages can pass `errorStepKey` into step list derivations.
  </action>
  <acceptance_criteria>
    - `grep -E "stepKey|errorStepKey" lib/hooks/useWorkflowStream.ts` exits 0
    - `grep -q "chunk.error" lib/hooks/useWorkflowStream.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && grep -E "stepKey|errorStepKey" lib/hooks/useWorkflowStream.ts | head -3</automated>
  </verify>
  <done>Hook consumers can read which step failed during an active or failed stream without parsing the error string.</done>
</task>

<task type="auto">
  <name>Task 3: Update handleWorkflowError + success clears in all four workflows; pass stepKey from failures</name>
  <read_first>
    - workflows/extract-cv.ts
    - workflows/analyze-job-posting.ts
    - workflows/positioning-analyze.ts
    - workflows/positioning-generate.ts
    - lib/types/workflow-last-error.ts
  </read_first>
  <files>
    workflows/extract-cv.ts
    workflows/analyze-job-posting.ts
    workflows/positioning-analyze.ts
    workflows/positioning-generate.ts
    lib/queries/candidates.ts
    lib/queries/positionings.ts
    lib/queries/missions.ts
  </files>
  <action>
**A. `handleWorkflowError` signatures**

- For `candidates` / `positionings`: add optional fourth argument `ctx?: { stepKey?: string }`.
- For `missions` (analyze-job-posting): add `ctx?: { stepKey?: string }` as second parameter after `missionId` (adjust call sites).

**B. DB write:** When handling error, set `workflow_last_error` to `{ stepKey: ctx?.stepKey ?? 'unknown', message: errorMessage }` using `workflowLastErrorSchema.parse` or safe shape. Always keep existing status / `workflow_run_id` clearing behavior from Phase 1.

**C. NDJSON frame:** Replace `{ error: errorMessage }` with JSON line that includes `stepKey` when known:
`JSON.stringify({ error: errorMessage, ...(ctx?.stepKey ? { stepKey: ctx.stepKey } : {}) }) + '\n'`

**D. Success clears:** Wherever the workflow sets terminal success (e.g. `saveResult`, mission analysis save), add `.update({ workflow_last_error: null })` **or** include `workflow_last_error: null` in the same update object if one already exists — per table.

**E. Pass `stepKey` from catch sites:** At minimum, wrap inner `'use step'` failures so the top-level catch calls `handleWorkflowError(..., { stepKey: 'transcription' })` etc. Use keys aligned with `02-CONTEXT.md` D-04 — reuse existing branch names from `lib/types/*-stream.ts` (`CvExtractionBranch`, etc.) and phase names `transcription` / `reading` / `extracting` for CV.

**F. Query types:** Extend selected row types in `lib/queries/candidates.ts`, `lib/queries/positionings.ts`, `lib/queries/missions.ts` so `workflow_last_error: WorkflowLastError | null` appears on fetched entities (import type from `lib/types/workflow-last-error.ts`).
  </action>
  <acceptance_criteria>
    - `grep -l "workflow_last_error" workflows/extract-cv.ts workflows/analyze-job-posting.ts workflows/positioning-analyze.ts workflows/positioning-generate.ts | wc -l` outputs `4`
    - `grep -q "stepKey" workflows/extract-cv.ts`
    - `grep -q "workflow_last_error" lib/queries/candidates.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && grep -c "workflow_last_error" workflows/extract-cv.ts workflows/positioning-analyze.ts && pnpm exec tsc --noEmit --pretty false 2>&1 | head -8</automated>
  </verify>
  <done>All four workflows persist and stream step-aware errors; success paths clear `workflow_last_error`; TypeScript knows the new column.</done>
</task>

</tasks>
