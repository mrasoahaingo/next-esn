---
phase: 02-sub-step-progress-step-error-attribution
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - app/review/[id]/page.tsx
  - app/review/components/ExtractionProgress.tsx
  - app/positions/[id]/page.tsx
  - components/mission-job-analysis.tsx
  - app/review/[id]/positioning/[positioningId]/page.tsx
  - app/review/[id]/positioning/[positioningId]/components/AnalysisView.tsx
  - app/review/[id]/positioning/[positioningId]/components/CvGenerationStep.tsx
  - app/review/[id]/positioning/[positioningId]/components/EmailsGenerationStep.tsx
autonomous: true
requirements: [SUB-01, SUB-02, ERR-03]

must_haves:
  truths:
    - "All four streaming surfaces render WorkflowStepList (or pass props into it) when a workflow is active or failed with persisted error"
    - "Error toasts include the failing step name in the first clause (D-09)"
    - "Review page CV extraction uses shared step list aligned with stream meta"
  artifacts:
    - path: "app/review/[id]/page.tsx"
      provides: "CV extraction step list + errorStepKey wiring"
    - path: "components/mission-job-analysis.tsx"
      provides: "Job posting analysis step list"
    - path: "app/review/[id]/positioning/[positioningId]/page.tsx"
      provides: "Positioning analysis + generation step lists"
  key_links:
    - from: "pages"
      to: "WorkflowStepList"
      via: "compute*StepStates + candidateRow.workflow_last_error"
---

<objective>
Wire **WorkflowStepList** and **compute*StepStates** into all four streaming UIs for parity (D-08), including Sonner toasts that name the failing step (ERR-03 + D-09).

Purpose: Deliver visible sub-step progress and step-level errors across the product.

Output: Updated pages/components; optional thin wrappers where props are assembled; **no** new feature scope beyond reliability UX.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-sub-step-progress-step-error-attribution/02-UI-SPEC.md
@lib/hooks/useWorkflowStream.ts
@lib/workflow/workflow-step-labels.ts
@lib/workflow/compute-step-status.ts
@components/workflow/WorkflowStepList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CV extraction — review page + ExtractionProgress alignment</name>
  <read_first>
    - app/review/[id]/page.tsx
    - app/review/components/ExtractionProgress.tsx
    - lib/workflow/compute-step-status.ts
    - components/workflow/WorkflowStepList.tsx
  </read_first>
  <files>app/review/[id]/page.tsx, app/review/components/ExtractionProgress.tsx</files>
  <action>
**Goal:** Single visible pattern for **SUB-01 / SUB-02** on `/review/[id]`.

1. From `useWorkflowStream` for CV extraction, read `streamMeta`, `isLoading`, `error`, and `errorStepKey` (from Plan 02-01).

2. Load `workflow_last_error` from the React Query candidate object (already fetched on page) when `status === 'error'` and stream is not active.

3. Call `computeCvStepStates` (or equivalent name from `compute-step-status.ts`) with merged inputs; pass rows + `summaryLine` to `WorkflowStepList`.

4. **Refactor `ExtractionProgress`:** Either replace internals with `WorkflowStepList` while keeping section-level progress if still needed, **or** remove duplicate vertical UI and delegate to `WorkflowStepList` only. Do not leave two competing progress UIs.

5. **Toast (ERR-03):** Where `toast.error` fires for extraction, prefix with step label resolved from `errorStepKey` or `workflow_last_error.stepKey` using `workflow-step-labels.ts` (e.g. `Transcription : échec. …`).

6. Ensure `aria-live` regions are not duplicated (single polite region).
  </action>
  <acceptance_criteria>
    - `grep -q "WorkflowStepList" app/review/[id]/page.tsx`
    - `grep -E "compute.*Step|computeCv" app/review/[id]/page.tsx` exits 0
    - `grep -q "toast.error" app/review/[id]/page.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && grep -q "WorkflowStepList" app/review/\[id\]/page.tsx && grep -E "compute.*Step|computeCv" app/review/\[id\]/page.tsx</automated>
  </verify>
  <done>Review route shows step list + step-aware error toast for extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Mission job posting analysis + mission CV extract/analysis page</name>
  <read_first>
    - components/mission-job-analysis.tsx
    - app/positions/[id]/page.tsx
    - lib/workflow/compute-step-status.ts
  </read_first>
  <files>components/mission-job-analysis.tsx, app/positions/[id]/page.tsx</files>
  <action>
**A. `components/mission-job-analysis.tsx`**

- Import `WorkflowStepList` + job posting compute function + labels.
- Replace `formatJobPostingStreamHint` **or** keep hint as secondary only if UI-SPEC allows — prefer **one** summary + list (D-07).
- Wire `stream.streamMeta`, `stream.error`, `errorStepKey`, mission row `workflow_last_error` from props/query.

**B. `app/positions/[id]/page.tsx`**

- For **both** `extractStream` and `analysisStream`, render a `WorkflowStepList` in the appropriate Card for each workflow when status is active or error.
- Remove redundant `formatCvExtractionHint` / `formatAnalysisStreamHint` one-liners if fully superseded by SUB-01 line; if kept, ensure they do not contradict the list.

- Pass `workflow_last_error` from candidate + positioning records per existing queries.

**Toast:** step-prefixed French error for each workflow failure path.
  </action>
  <acceptance_criteria>
    - `grep -q "WorkflowStepList" components/mission-job-analysis.tsx`
    - `grep -q "WorkflowStepList" app/positions/[id]/page.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && grep -c "WorkflowStepList" components/mission-job-analysis.tsx app/positions/\[id\]/page.tsx</automated>
  </verify>
  <done>Mission surfaces show job posting + CV extract + positioning analysis step UX per parity.</done>
</task>

<task type="auto">
  <name>Task 3: Positioning detail — analysis + generation steps</name>
  <read_first>
    - app/review/[id]/positioning/[positioningId]/page.tsx
    - app/review/[id]/positioning/[positioningId]/components/AnalysisView.tsx
    - app/review/[id]/positioning/[positioningId]/components/CvGenerationStep.tsx
    - app/review/[id]/positioning/[positioningId]/components/EmailsGenerationStep.tsx
  </read_first>
  <files>
    app/review/[id]/positioning/[positioningId]/page.tsx
    app/review/[id]/positioning/[positioningId]/components/AnalysisView.tsx
    app/review/[id]/positioning/[positioningId]/components/CvGenerationStep.tsx
    app/review/[id]/positioning/[positioningId]/components/EmailsGenerationStep.tsx
  </files>
  <action>
**Analysis:** `AnalysisView` already consumes `streamMeta` — integrate `WorkflowStepList` at the top of the analysis Card (or inside `AnalysisView`) using `computePositioningAnalysisStepStates`. Preserve existing section-level detail below; step list is the **global** progress anchor (D-06).

**Generation:** `CvGenerationStep` and `EmailsGenerationStep` — add `WorkflowStepList` for `positioningGenerate` when `generateMode` / `activeBranches` indicate streaming; share compute function for generation.

**Page shell:** `page.tsx` wires `errorStepKey` from both `useWorkflowStream` instances; passes `positioning.workflow_last_error` when status is `error`.

**Toast:** `toast.error` messages must include the French step label for failing analysis or generation (resolve from `workflow-step-labels.ts`).

**Cleanup:** Remove obsolete subtitle strings that duplicate SUB-01 if fully replaced by `WorkflowStepList` summary line.
  </action>
  <acceptance_criteria>
    - `grep -q "WorkflowStepList" app/review/[id]/positioning/[positioningId]/page.tsx`
    - `grep -q "WorkflowStepList" app/review/[id]/positioning/[positioningId]/components/AnalysisView.tsx`
    - `grep -q "WorkflowStepList" app/review/[id]/positioning/[positioningId]/components/CvGenerationStep.tsx`
    - `grep -q "WorkflowStepList" app/review/[id]/positioning/[positioningId]/components/EmailsGenerationStep.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && rg -l "WorkflowStepList" app/review/\[id\]/positioning/\[positioningId\]/ | wc -l</automated>
  </verify>
  <done>Positioning analysis + CV/email generation show unified step progress + step errors.</done>
</task>

</tasks>
