# Quick task 260326-x7n — Plan

## Goal

Corriger l’affichage des étapes d’analyse de fiche mission lorsque le run serveur est actif mais le flux NDJSON n’a pas encore reconnecté (`isStreaming === false`), qui déclenchait le raccourci « toutes les étapes terminées » dans `computeJobPostingStepStates`.

## Tasks

1. Ajouter `workflowRunActive?: boolean` à `computeJobPostingStepStates` et n’appliquer le raccourci « tout done » que si ce flag est faux.
2. Passer `workflowRunActive: !!job_analysis_workflow_run_id` depuis `PositioningMissionAnalysisInline` et `MissionJobAnalysis`.
