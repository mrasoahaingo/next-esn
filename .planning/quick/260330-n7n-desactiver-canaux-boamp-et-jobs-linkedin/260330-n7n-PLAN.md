---
phase: quick-260330-n7n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/radar/cron/collect-signals/route.ts
  - lib/radar/collectors/linkedin-browser.ts
  - app/api/radar/workflows/collect-linkedin.workflow.ts
autonomous: true
requirements: [N7N-01, N7N-02]

must_haves:
  truths:
    - "Le cron ne lance plus les workflows boamp et jobs"
    - "Le collecteur LinkedIn browser scrape les pages entreprises et LinkedIn Jobs via Stagehand"
    - "Le workflow collect-linkedin appelle les deux collecteurs (Proxycurl + Stagehand browser) en parallèle"
    - "Les signaux des deux sources sont fusionnés sans doublons dans le workflow"
  artifacts:
    - path: "app/api/radar/cron/collect-signals/route.ts"
      provides: "Cron sans boamp/jobs dans startCollectRuns"
      contains: "collectPressWorkflow, collectLinkedInWorkflow"
    - path: "lib/radar/collectors/linkedin-browser.ts"
      provides: "Collecteur Stagehand pour pages entreprise + LinkedIn Jobs"
      exports: ["collectLinkedInBrowserSignals"]
    - path: "app/api/radar/workflows/collect-linkedin.workflow.ts"
      provides: "Workflow orchestrant Proxycurl + Stagehand browser"
      contains: "collectLinkedInSignals, collectLinkedInBrowserSignals"
  key_links:
    - from: "app/api/radar/cron/collect-signals/route.ts"
      to: "app/api/radar/workflows/collect-linkedin.workflow.ts"
      via: "start(collectLinkedInWorkflow, [orgId])"
      pattern: "collectLinkedInWorkflow"
    - from: "app/api/radar/workflows/collect-linkedin.workflow.ts"
      to: "lib/radar/collectors/linkedin-browser.ts"
      via: "collectLinkedInBrowserSignals(urls)"
      pattern: "collectLinkedInBrowserSignals"
---

<objective>
Désactiver les canaux boamp et jobs dans le cron de collecte, et ajouter un collecteur LinkedIn browser (Stagehand) en parallèle du Proxycurl existant.

Purpose: Boamp et jobs sont trop instables/coûteux pour le cron; LinkedIn browser apporte des signaux complémentaires (pages entreprise + offres jobs) sans remplacer Proxycurl.
Output: Cron allégé (press + linkedin uniquement), nouveau collecteur `linkedin-browser.ts`, workflow linkedin fusionnant les deux sources.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260330-n7n-desactiver-canaux-boamp-et-jobs-linkedin/260330-n7n-CONTEXT.md

@lib/radar/collectors/jobs.ts
@lib/radar/collectors/linkedin.ts
@app/api/radar/workflows/collect-linkedin.workflow.ts
@app/api/radar/cron/collect-signals/route.ts

<interfaces>
<!-- Contrats clés pour l'exécuteur — extraits de la codebase -->

From lib/radar/schemas.ts:
```typescript
export const RawSignalSchema = z.object({ ... });
export type RawSignal = z.infer<typeof RawSignalSchema>;
export type ApiCall = { endpoint: string; status: number; ok: boolean; responseData: unknown };
export const JobOfferExtractionSchema = z.object({ offers: z.array(JobOfferItemSchema) });
```

From lib/radar/collectors/jobs.ts — pattern Stagehand à réutiliser:
```typescript
function createStagehand() {
  return new Stagehand({
    env: 'BROWSERBASE',
    apiKey: process.env.BROWSERBASE_API_KEY!,
    projectId: process.env.BROWSERBASE_PROJECT_ID!,
    modelName: 'gpt-4o-mini',
    modelClientOptions: { apiKey: process.env.OPENAI_API_KEY! },
    verbose: 0,
  });
}
// Pattern : stagehand.init() → stagehand.page.goto() → stagehand.extract({ instruction, schema }) → stagehand.close()
```

From app/api/radar/workflows/collect-linkedin.workflow.ts — signature attendue par le workflow:
```typescript
// La fonction step fetchLinkedInSignals(urls) retourne { signals: RawSignal[]; calls: ApiCall[] }
// Le workflow log via insertRunLog(orgId, 'linkedin', { collected, persisted, urlCount, calls })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Désactiver boamp et jobs dans le cron</name>
  <files>app/api/radar/cron/collect-signals/route.ts</files>
  <action>
    Dans `startCollectRuns`, retirer les deux entrées boamp et jobs du `Promise.all` :
    - Supprimer `start(collectJobsWorkflow, [orgId]).then(...)`
    - Supprimer `start(collectBoampWorkflow, [orgId]).then(...)`
    - Conserver uniquement `collectPressWorkflow` et `collectLinkedInWorkflow`

    Supprimer les imports `collectJobsWorkflow` et `collectBoampWorkflow` en haut du fichier.
    Les fichiers workflow `collect-boamp.workflow.ts` et `collect-jobs.workflow.ts` restent intacts — pas de suppression.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "collect-signals" || echo "OK"</automated>
  </verify>
  <done>Le cron ne référence plus boamp ni jobs. `startCollectRuns` ne contient plus que press et linkedin.</done>
</task>

<task type="auto">
  <name>Task 2: Créer le collecteur LinkedIn browser (Stagehand)</name>
  <files>lib/radar/collectors/linkedin-browser.ts</files>
  <action>
    Créer `lib/radar/collectors/linkedin-browser.ts` avec la fonction exportée `collectLinkedInBrowserSignals(companyUrls: string[]): Promise<{ signals: RawSignal[]; calls: ApiCall[] }>`.

    Réutiliser exactement le pattern `createStagehand()` de `jobs.ts` (même config : BROWSERBASE env, gpt-4o-mini, OPENAI_API_KEY).
    Guard en début de fonction : si `!BROWSERBASE_API_KEY || !BROWSERBASE_PROJECT_ID` → retourner `{ signals: [], calls: [] }`.

    Structure `try { await stagehand.init(); ... } finally { await stagehand.close(); }` — même pattern que jobs.ts.

    **Bloc A — Pages entreprise LinkedIn** (pour chaque `companyUrl`) :
    - Naviguer sur `companyUrl` (URL LinkedIn de l'entreprise, ex: `https://linkedin.com/company/xxx`)
    - `stagehand.extract({ instruction: "Extrais les offres d'emploi IT visibles sur cette page LinkedIn (poste, type de contrat, technologies). Détecte aussi la mention de prestataires ou consultants externes.", schema: LinkedInCompanyPageSchema })`
    - Créer un `RawSignal` source `'linkedin'`, title `"N offres IT détectées (browser)"`, companyName extrait de la page ou déduit de l'URL, weight selon le nombre d'offres (même barème que jobs.ts : >=5→25, >=3→20, >=2→15, sinon 10)
    - Loguer via `console.info('[radar][linkedin-browser]', event, payload)`

    **Bloc B — LinkedIn Jobs search** :
    - Une seule session de navigation (pas de boucle par entreprise)
    - Naviguer sur `https://www.linkedin.com/jobs/search/?keywords=consultant%20IT%20développeur%20cloud%20data&location=France`
    - `stagehand.extract({ instruction: "Extrais toutes les offres d'emploi IT visibles : titre, entreprise, lieu, compétences, type de contrat, URL.", schema: LinkedInJobsExtractionSchema })`
    - Grouper par entreprise (même logique que `jobs.ts` avec `Object.groupBy`), créer un `RawSignal` par entreprise, source `'job_offer'`, weight selon nombre d'offres

    **Schémas Zod locaux** (définir dans ce fichier, ne pas exporter dans schemas.ts) :
    ```typescript
    const LinkedInCompanyPageSchema = z.object({
      jobCount: z.number().default(0),
      jobTitles: z.array(z.string()).default([]),
      technologies: z.array(z.string()).default([]),
      hasExternalConsultants: z.boolean().default(false),
      companyName: z.string().optional(),
    });

    const LinkedInJobsItemSchema = z.object({
      title: z.string(),
      company: z.string(),
      location: z.string().optional(),
      contractType: z.string().optional(),
      skills: z.array(z.string()).default([]),
      url: z.string().optional(),
    });

    const LinkedInJobsExtractionSchema = z.object({
      offers: z.array(LinkedInJobsItemSchema).default([]),
    });
    ```

    Fusionner les signaux des deux blocs dans le tableau `signals` retourné. Pas de déduplication à ce niveau (le workflow gère via `upsertSignals`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "linkedin-browser" || echo "OK"</automated>
  </verify>
  <done>
    `lib/radar/collectors/linkedin-browser.ts` existe, exporte `collectLinkedInBrowserSignals`, compile sans erreur TypeScript.
    Les deux blocs (pages entreprise + jobs search) sont implémentés avec le pattern Stagehand.
  </done>
</task>

<task type="auto">
  <name>Task 3: Intégrer le collecteur browser dans le workflow LinkedIn</name>
  <files>app/api/radar/workflows/collect-linkedin.workflow.ts</files>
  <action>
    Modifier `collect-linkedin.workflow.ts` pour appeler les deux collecteurs en parallèle et fusionner leurs signaux.

    1. Ajouter l'import : `import { collectLinkedInBrowserSignals } from '@/lib/radar/collectors/linkedin-browser';`

    2. Remplacer la step `fetchLinkedInSignals` par une step qui appelle les deux collecteurs en parallèle :
    ```typescript
    async function fetchAllLinkedInSignals(urls: string[]) {
      'use step';

      const [proxycurl, browser] = await Promise.all([
        collectLinkedInSignals(urls),
        collectLinkedInBrowserSignals(urls),
      ]);

      return {
        signals: [...proxycurl.signals, ...browser.signals],
        calls: [...proxycurl.calls, ...browser.calls],
      };
    }
    ```

    3. Dans `collectLinkedInWorkflow`, remplacer l'appel `fetchLinkedInSignals(urls)` par `fetchAllLinkedInSignals(urls)`.

    4. Le reste du workflow (persistLinkedInSignals, logLinkedInRun) reste identique — la fusion est transparente.

    Note : `upsertSignals` gère la déduplication via upsert — pas besoin de déduplication manuelle avant persistence.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "collect-linkedin" || echo "OK"</automated>
  </verify>
  <done>
    Le workflow appelle `fetchAllLinkedInSignals` qui exécute Proxycurl et Stagehand en parallèle.
    Les signaux sont fusionnés avant persistence. TypeScript compile sans erreur.
  </done>
</task>

</tasks>

<verification>
Vérification globale après les 3 tasks :

```bash
npx tsc --noEmit
```

Doit retourner 0 erreur.

Vérification manuelle du cron (optionnel) :
```bash
# Le cron ne doit lister que press et linkedin
grep -n "collectJobs\|collectBoamp\|collectPress\|collectLinkedIn" app/api/radar/cron/collect-signals/route.ts
```
Doit montrer uniquement press et linkedin.
</verification>

<success_criteria>
- `startCollectRuns` dans `collect-signals/route.ts` ne contient plus boamp ni jobs
- `lib/radar/collectors/linkedin-browser.ts` existe et exporte `collectLinkedInBrowserSignals`
- `collect-linkedin.workflow.ts` appelle les deux collecteurs en parallèle via `Promise.all`
- `npx tsc --noEmit` passe sans erreur
</success_criteria>

<output>
After completion, create `.planning/quick/260330-n7n-desactiver-canaux-boamp-et-jobs-linkedin/260330-n7n-SUMMARY.md`
</output>
