---
phase: quick-260605-e30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605100936_cv_transcription_language_neutral.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "Importing an English PDF CV produces extracted text in English, not French"
    - "detectCvLanguage correctly detects 'en' from English transcription output"
    - "All 4 branch prompts inject language_label='English' for English CVs"
  artifacts:
    - path: "supabase/migrations/20260605100936_cv_transcription_language_neutral.sql"
      provides: "Removes 'français' instruction from cv.transcription system prompt"
      contains: "UPDATE llm_tasks"
  key_links:
    - from: "cv.transcription system prompt"
      to: "cvText (parallelExtractAndStream input)"
      via: "LLM transcription output fed to detectCvLanguage"
      pattern: "detectCvLanguage(cvText)"
---

<objective>
Fix English CV being translated to French during extraction.

Purpose: The cv.transcription LLM prompt contains "Consigne : français" which forces the LLM to output French regardless of the source document language. This means detectCvLanguage runs on already-translated French text, returns 'fr', and all 4 branch prompts inject language_label='Français' — so even a fully English CV is extracted in French.

Output: DB migration updating cv.transcription system prompt to be language-neutral (transcribe faithfully in the document's original language).
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Key context: how the pipeline works
# 1. prepareCvText (PDF path): LLM transcribes the PDF using cv.transcription system prompt
# 2. The transcribed text becomes cvText
# 3. parallelExtractAndStream: detectCvLanguage(cvText) — detects language from transcribed text
# 4. langContext = { language: detectedLang, language_label } injected into all 4 branch prompts
#
# Bug: cv.transcription prompt says "Consigne : français" → LLM outputs French
# → detectCvLanguage sees French text → returns 'fr'
# → branches get language_label='Français' even for English CVs
#
# Fix: remove "français" constraint from transcription prompt.
# Transcription must preserve the source language faithfully — it's a faithful copy, not a translation.
#
# Current prompt (in llm_tasks table):
# 'Tu transcris un CV depuis le document PDF en texte structuré.
# Consigne : français, fidélité maximale au contenu, sans inventer d''information.
# Préserve la structure : titres de sections, puces, dates, noms d''entreprises et de formations.
# Ne produis pas de JSON ; uniquement du texte brut ou markdown léger (titres ##).'
#
# Fixed prompt: remove "français," → preserve the document's original language
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration — make cv.transcription language-neutral</name>
  <files>supabase/migrations/20260605100936_cv_transcription_language_neutral.sql</files>
  <action>
    Create a migration that updates the cv.transcription system prompt in llm_tasks.
    Replace "Consigne : français, fidélité maximale au contenu" with "Consigne : fidélité maximale au contenu, dans la langue d'origine du document" (faithfulness to source language, not French).

    Full new system prompt text:
    ```
    Tu transcris un CV depuis le document PDF en texte structuré.
    Consigne : fidélité maximale au contenu, dans la langue d'origine du document, sans inventer d''information.
    Préserve la structure : titres de sections, puces, dates, noms d''entreprises et de formations.
    Ne produis pas de JSON ; uniquement du texte brut ou markdown léger (titres ##).
    ```

    Migration must be idempotent — use WHERE to target only the cv.transcription row.
    Follow the migration timestamp format already used: 14-digit (20260605100936).
  </action>
  <verify>
    <automated>ls supabase/migrations/20260605100936_cv_transcription_language_neutral.sql && grep -q "langue d'origine" supabase/migrations/20260605100936_cv_transcription_language_neutral.sql && echo "OK"</automated>
  </verify>
  <done>Migration file exists, contains the updated prompt with "langue d'origine" replacing "français", idempotent UPDATE statement targeting task_key = 'cv.transcription'.</done>
</task>

</tasks>

<verification>
After migration is applied (via Supabase dashboard or supabase db push):
1. Upload an English PDF CV (e.g., Resume Ana Casas 2026.pdf)
2. Extraction completes — verify extracted text fields (name, summary, experience descriptions) are in English, not French
3. candidates.language column in DB is 'en' for this candidate
</verification>

<success_criteria>
- Migration file created with correct timestamp and idempotent UPDATE
- cv.transcription system prompt no longer instructs French output
- English CVs transcribed in English → detectCvLanguage returns 'en' → branches extract in English
</success_criteria>

<output>
After completion, create `.planning/quick/260605-e30-cv-import-en-anglais-traduit-en-fran-ais/260605-e30-SUMMARY.md`
</output>
