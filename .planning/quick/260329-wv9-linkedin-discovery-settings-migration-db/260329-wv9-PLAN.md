---
phase: quick
plan: 260329-wv9
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260329_003_radar_linkedin_discovery.sql
  - lib/radar/settings.ts
  - app/(dashboard)/radar/components/radar-settings-form.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "La colonne linkedin_discovery existe dans radar_org_settings"
    - "Les settings sont lus et ecrits avec linkedin_discovery en base"
    - "Le formulaire affiche et sauvegarde tous les champs LinkedIn Discovery"
  artifacts:
    - path: "supabase/migrations/20260329_003_radar_linkedin_discovery.sql"
      provides: "ALTER TABLE pour ajouter la colonne linkedin_discovery JSONB"
    - path: "lib/radar/settings.ts"
      provides: "radarSettingsPatchSchema, DEFAULT_RADAR_SETTINGS, mapRowToSettings, upsertRadarSettings mis a jour"
    - path: "app/(dashboard)/radar/components/radar-settings-form.tsx"
      provides: "Section LinkedIn Discovery dans le formulaire"
  key_links:
    - from: "radar-settings-form.tsx"
      to: "upsertRadarSettings"
      via: "PATCH /api/radar/settings avec linkedinDiscovery dans le payload"
    - from: "mapRowToSettings"
      to: "row.linkedin_discovery"
      via: "radarSettingsSchema.parse avec linkedin_discovery"
---

<objective>
Brancher linkedin_discovery de bout en bout : migration DB, couche settings (schema patch, defaults, read/write), et section UI dans RadarSettingsForm.

Purpose: Le schema linkedinDiscoverySchema et DEFAULT_LINKEDIN_DISCOVERY existent deja, mais la colonne DB manque et les fonctions settings ne lisent/ecrivent pas ce champ. Le formulaire n'a pas de section pour le configurer.
Output: Migration SQL + settings.ts mis a jour + section UI dans RadarSettingsForm
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mrasoahaingo/Projects/perso/next-esn/lib/radar/settings.ts
@/Users/mrasoahaingo/Projects/perso/next-esn/app/(dashboard)/radar/components/radar-settings-form.tsx
@/Users/mrasoahaingo/Projects/perso/next-esn/supabase/migrations/20260419_radar_org_settings.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration DB — ajouter colonne linkedin_discovery</name>
  <files>supabase/migrations/20260329_003_radar_linkedin_discovery.sql</files>
  <action>
    Creer le fichier de migration suivant :

    ```sql
    ALTER TABLE radar_org_settings
      ADD COLUMN IF NOT EXISTS linkedin_discovery JSONB NOT NULL DEFAULT '{
        "enabled": false,
        "sectors": ["banque","assurance","retail","sante","industrie","services publics"],
        "regions": ["Ile-de-France","Lyon","Bordeaux","Nantes","Marseille"],
        "keywords": ["informatique","digital","IT","DSI","transformation numerique"],
        "minExternalRatio": 0.05,
        "minHeadcount": 200,
        "maxHeadcount": 10000,
        "maxCompaniesPerRun": 50
      }'::jsonb;
    ```

    La valeur DEFAULT doit correspondre exactement a DEFAULT_LINKEDIN_DISCOVERY dans lib/radar/settings.ts.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260329_003_radar_linkedin_discovery.sql && grep -q "linkedin_discovery" supabase/migrations/20260329_003_radar_linkedin_discovery.sql && echo "OK"</automated>
  </verify>
  <done>Le fichier de migration existe et contient l'ALTER TABLE avec la colonne linkedin_discovery JSONB NOT NULL DEFAULT.</done>
</task>

<task type="auto">
  <name>Task 2: settings.ts — brancher linkedin_discovery dans patch schema, defaults, read et write</name>
  <files>lib/radar/settings.ts</files>
  <action>
    Quatre modifications dans lib/radar/settings.ts :

    1. **radarSettingsPatchSchema** — ajouter le champ linkedinDiscovery optionnel :
    ```typescript
    export const radarSettingsPatchSchema = z.object({
      enabledSources: radarSettingsSchema.shape.enabledSources.optional(),
      jobSearchQueries: z.array(z.string().min(1)).optional(),
      pressRssUrls: z.array(z.string().url()).optional(),
      linkedinCompanyUrls: z.array(z.string().url()).optional(),
      linkedinDiscovery: linkedinDiscoverySchema.optional(),
      matchThreshold: z.number().min(0).max(1).optional(),
    });
    ```

    2. **DEFAULT_RADAR_SETTINGS** — ajouter linkedinDiscovery :
    ```typescript
    export const DEFAULT_RADAR_SETTINGS: Omit<RadarSettings, 'orgId'> = {
      // ... champs existants ...
      linkedinDiscovery: DEFAULT_LINKEDIN_DISCOVERY,
      updatedAt: null,
    };
    ```

    3. **mapRowToSettings** — lire row.linkedin_discovery :
    ```typescript
    function mapRowToSettings(orgId: string, row: Record<string, unknown> | null | undefined): RadarSettings {
      return radarSettingsSchema.parse({
        orgId,
        enabledSources: row?.enabled_sources ?? DEFAULT_RADAR_SETTINGS.enabledSources,
        jobSearchQueries: row?.job_search_queries ?? DEFAULT_RADAR_SETTINGS.jobSearchQueries,
        pressRssUrls: row?.press_rss_urls ?? DEFAULT_RADAR_SETTINGS.pressRssUrls,
        linkedinCompanyUrls: row?.linkedin_company_urls ?? DEFAULT_RADAR_SETTINGS.linkedinCompanyUrls,
        linkedinDiscovery: row?.linkedin_discovery ?? DEFAULT_RADAR_SETTINGS.linkedinDiscovery,
        matchThreshold: row?.match_threshold ?? DEFAULT_RADAR_SETTINGS.matchThreshold,
        updatedAt: row?.updated_at ?? null,
      });
    }
    ```

    4. **upsertRadarSettings** — inclure linkedin_discovery dans le upsert. Dans la construction de next :
    ```typescript
    const next = radarSettingsSchema.parse({
      orgId,
      enabledSources: patch.enabledSources ?? current.enabledSources,
      jobSearchQueries: patch.jobSearchQueries ?? current.jobSearchQueries,
      pressRssUrls: patch.pressRssUrls ?? current.pressRssUrls,
      linkedinCompanyUrls: patch.linkedinCompanyUrls ?? current.linkedinCompanyUrls,
      linkedinDiscovery: patch.linkedinDiscovery ?? current.linkedinDiscovery,
      matchThreshold: patch.matchThreshold ?? current.matchThreshold,
      updatedAt: new Date().toISOString(),
    });
    ```
    Et dans l'objet upsert Supabase, ajouter :
    ```typescript
    linkedin_discovery: next.linkedinDiscovery,
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compile sans erreur. radarSettingsPatchSchema accepte linkedinDiscovery, DEFAULT_RADAR_SETTINGS l'inclut, mapRowToSettings le lit, upsertRadarSettings l'ecrit en base.</done>
</task>

<task type="auto">
  <name>Task 3: RadarSettingsForm — section LinkedIn Discovery</name>
  <files>app/(dashboard)/radar/components/radar-settings-form.tsx</files>
  <action>
    Quatre zones a modifier dans RadarSettingsForm :

    1. **Import** — ajouter DEFAULT_LINKEDIN_DISCOVERY et LinkedInDiscovery :
    ```typescript
    import { DEFAULT_LINKEDIN_DISCOVERY, DEFAULT_RADAR_SETTINGS, radarSettingsSchema, type LinkedInDiscovery, type RadarSettings } from '@/lib/radar/settings';
    ```

    2. **SettingsFormState** — ajouter le champ linkedinDiscovery :
    ```typescript
    type SettingsFormState = {
      enabledSources: RadarSettings['enabledSources'];
      jobSearchQueriesText: string;
      pressRssUrlsText: string;
      linkedinCompanyUrlsText: string;
      matchThreshold: string;
      linkedinDiscovery: LinkedInDiscovery;
    };
    ```

    3. **useState initial + useEffect** — initialiser et synchroniser linkedinDiscovery :
    - Dans useState : ajouter `linkedinDiscovery: DEFAULT_LINKEDIN_DISCOVERY`
    - Dans useEffect (settingsQuery.data) : ajouter `linkedinDiscovery: settingsQuery.data.linkedinDiscovery`

    4. **handleSave payload** — ajouter linkedinDiscovery :
    ```typescript
    const payload = {
      enabledSources: form.enabledSources,
      jobSearchQueries: fromLines(form.jobSearchQueriesText),
      pressRssUrls: fromLines(form.pressRssUrlsText),
      linkedinCompanyUrls: fromLines(form.linkedinCompanyUrlsText),
      linkedinDiscovery: form.linkedinDiscovery,
      matchThreshold: Number(form.matchThreshold),
    };
    ```

    5. **Section UI** — inserer une Card "Decouverte LinkedIn" apres la Card "Seed LinkedIn" et avant la Card "Matching vivier" :

    ```tsx
    <Card className="border-border/70 bg-card/80 shadow-sm backdrop-blur-sm">
      <CardHeader>
        <CardTitle>Decouverte LinkedIn</CardTitle>
      </CardHeader>
      <CardContent className="space-y-4">
        <div className="flex items-center justify-between rounded-lg border border-border/60 bg-muted/20 p-4">
          <div>
            <Label>Activer la decouverte automatique</Label>
            <p className="text-sm text-muted-foreground">
              Recherche de nouvelles entreprises via LinkedIn selon les filtres ci-dessous.
            </p>
          </div>
          <Switch
            checked={form.linkedinDiscovery.enabled}
            onCheckedChange={(checked) =>
              setForm((current) => ({
                ...current,
                linkedinDiscovery: { ...current.linkedinDiscovery, enabled: checked },
              }))
            }
          />
        </div>

        <div className="space-y-3">
          <Label htmlFor="ldSectors">Secteurs cibles (un par ligne)</Label>
          <textarea
            id="ldSectors"
            className="min-h-28 w-full rounded-md border border-input bg-background/80 px-3 py-2 text-sm shadow-sm"
            value={toLines(form.linkedinDiscovery.sectors)}
            onChange={(event) =>
              setForm((current) => ({
                ...current,
                linkedinDiscovery: { ...current.linkedinDiscovery, sectors: fromLines(event.target.value) },
              }))
            }
          />
        </div>

        <div className="space-y-3">
          <Label htmlFor="ldRegions">Regions cibles (une par ligne)</Label>
          <textarea
            id="ldRegions"
            className="min-h-28 w-full rounded-md border border-input bg-background/80 px-3 py-2 text-sm shadow-sm"
            value={toLines(form.linkedinDiscovery.regions)}
            onChange={(event) =>
              setForm((current) => ({
                ...current,
                linkedinDiscovery: { ...current.linkedinDiscovery, regions: fromLines(event.target.value) },
              }))
            }
          />
        </div>

        <div className="space-y-3">
          <Label htmlFor="ldKeywords">Mots-cles (un par ligne)</Label>
          <textarea
            id="ldKeywords"
            className="min-h-28 w-full rounded-md border border-input bg-background/80 px-3 py-2 text-sm shadow-sm"
            value={toLines(form.linkedinDiscovery.keywords)}
            onChange={(event) =>
              setForm((current) => ({
                ...current,
                linkedinDiscovery: { ...current.linkedinDiscovery, keywords: fromLines(event.target.value) },
              }))
            }
          />
        </div>

        <div className="grid gap-4 sm:grid-cols-2">
          <div className="space-y-2">
            <Label htmlFor="ldMinExternalRatio">Ratio externes minimum (0–1)</Label>
            <Input
              id="ldMinExternalRatio"
              type="number"
              step="0.01"
              min="0"
              max="1"
              value={form.linkedinDiscovery.minExternalRatio}
              onChange={(event) =>
                setForm((current) => ({
                  ...current,
                  linkedinDiscovery: {
                    ...current.linkedinDiscovery,
                    minExternalRatio: Number(event.target.value),
                  },
                }))
              }
            />
          </div>
          <div className="space-y-2">
            <Label htmlFor="ldMaxCompanies">Max entreprises par run</Label>
            <Input
              id="ldMaxCompanies"
              type="number"
              step="1"
              min="1"
              max="200"
              value={form.linkedinDiscovery.maxCompaniesPerRun}
              onChange={(event) =>
                setForm((current) => ({
                  ...current,
                  linkedinDiscovery: {
                    ...current.linkedinDiscovery,
                    maxCompaniesPerRun: Number(event.target.value),
                  },
                }))
              }
            />
          </div>
          <div className="space-y-2">
            <Label htmlFor="ldMinHeadcount">Effectif minimum</Label>
            <Input
              id="ldMinHeadcount"
              type="number"
              step="1"
              min="1"
              value={form.linkedinDiscovery.minHeadcount}
              onChange={(event) =>
                setForm((current) => ({
                  ...current,
                  linkedinDiscovery: {
                    ...current.linkedinDiscovery,
                    minHeadcount: Number(event.target.value),
                  },
                }))
              }
            />
          </div>
          <div className="space-y-2">
            <Label htmlFor="ldMaxHeadcount">Effectif maximum</Label>
            <Input
              id="ldMaxHeadcount"
              type="number"
              step="1"
              min="1"
              value={form.linkedinDiscovery.maxHeadcount}
              onChange={(event) =>
                setForm((current) => ({
                  ...current,
                  linkedinDiscovery: {
                    ...current.linkedinDiscovery,
                    maxHeadcount: Number(event.target.value),
                  },
                }))
              }
            />
          </div>
        </div>
      </CardContent>
    </Card>
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compile sans erreur. Le formulaire affiche la section "Decouverte LinkedIn" avec toggle enabled, textareas sectors/regions/keywords, et inputs minExternalRatio/minHeadcount/maxHeadcount/maxCompaniesPerRun. Le payload handleSave inclut linkedinDiscovery.</done>
</task>

</tasks>

<verification>
- Migration SQL presente et syntaxiquement correcte (ALTER TABLE ... ADD COLUMN IF NOT EXISTS)
- `npx tsc --noEmit` passe sans erreur
- lib/radar/settings.ts : radarSettingsPatchSchema inclut linkedinDiscovery, DEFAULT_RADAR_SETTINGS l'inclut, mapRowToSettings lit row.linkedin_discovery, upsertRadarSettings ecrit linkedin_discovery
- RadarSettingsForm : section UI visible, payload save inclut linkedinDiscovery
</verification>

<success_criteria>
- Colonne linkedin_discovery existe en DB (migration appliquee)
- GET /api/radar/settings retourne linkedinDiscovery
- PATCH /api/radar/settings avec { linkedinDiscovery: {...} } persiste le champ
- La section "Decouverte LinkedIn" est visible et fonctionnelle dans /radar/settings
</success_criteria>

<output>
Apres completion, creer `.planning/quick/260329-wv9-linkedin-discovery-settings-migration-db/260329-wv9-SUMMARY.md`
</output>
