---
phase: quick-260701-1wi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/marketing/landing.tsx
  - components/auth/auth-brand-panel.tsx
  - components/auth/auth-screen.tsx
  - app/sign-in/[[...sign-in]]/page.tsx
  - app/sign-up/[[...sign-up]]/page.tsx
autonomous: true
requirements: [QUICK-260701-1wi]

must_haves:
  truths:
    - "Sur la landing, un visiteur non connecté voit 'Se connecter' / 'Démo' / 'Créer un compte'"
    - "Sur la landing, un utilisateur connecté voit un lien vers /dashboard (au lieu des liens auth)"
    - "Les pages /sign-in et /sign-up s'affichent en 2 colonnes (panneau de marque à gauche, formulaire Clerk à droite) sur desktop"
    - "Sur mobile, les pages auth affichent un lien retour vers / + le logo Esneo, puis le formulaire centré"
  artifacts:
    - path: "components/marketing/landing.tsx"
      provides: "Liens conditionnels SignedIn/SignedOut dans SiteNav, Hero, FinalCta, SiteFooter"
      contains: "SignedIn"
    - path: "components/auth/auth-brand-panel.tsx"
      provides: "Panneau de marque gauche statique (kicker, titre serif, lede, retour /)"
      contains: "EsneoFullLogo"
    - path: "components/auth/auth-screen.tsx"
      provides: "Coquille 2 colonnes + borderlessAppearance pour Clerk"
      contains: "borderlessAppearance"
    - path: "app/sign-in/[[...sign-in]]/page.tsx"
      provides: "SignIn dans AuthScreen avec textes ESN"
      contains: "AuthScreen"
    - path: "app/sign-up/[[...sign-up]]/page.tsx"
      provides: "SignUp dans AuthScreen avec textes ESN"
      contains: "AuthScreen"
  key_links:
    - from: "components/marketing/landing.tsx"
      to: "@clerk/nextjs"
      via: "import { SignedIn, SignedOut }"
      pattern: "SignedIn|SignedOut"
    - from: "app/sign-in/[[...sign-in]]/page.tsx"
      to: "components/auth/auth-screen.tsx"
      via: "AuthScreen wrapping SignIn with appearance={borderlessAppearance}"
      pattern: "borderlessAppearance"
---

<objective>
Deux changements UI liés à l'authentification :
1. La landing affiche des liens vers `/dashboard` quand l'utilisateur est connecté (au lieu des liens "Se connecter" / "Démo" / "Créer un compte"), via les composants Clerk `<SignedIn>` / `<SignedOut>`.
2. Les pages `/sign-in` et `/sign-up` passent en layout 2 colonnes (split éditorial) inspiré de legal-advisor-juris-gps, mais ADAPTÉ à Esneo : sobre, statique (pas de `motion/react`, pas de RouteMap), avec uniquement les tokens de thème existants et le logo `EsneoFullLogo`.

Purpose: Cohérence de navigation (un utilisateur connecté n'a pas à re-cliquer "Se connecter") et pages auth qui ne ressemblent pas à des pages orphelines.
Output: landing.tsx mis à jour + nouveau dossier components/auth/ + pages sign-in/sign-up refondues.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@components/marketing/landing.tsx
@app/sign-in/[[...sign-in]]/page.tsx
@app/sign-up/[[...sign-up]]/page.tsx
@components/auth-brand-logo.tsx
@components/esneo-full-logo.tsx

<interfaces>
<!-- Tokens de thème VÉRIFIÉS disponibles dans next-esn (app/globals.css + tokens.css). NE PAS inventer d'autres tokens. -->
Disponibles : bg-background, text-foreground, bg-card, bg-secondary, bg-primary, text-primary-foreground,
text-muted-foreground, border-border, ring-ring, et leurs dérivés d'opacité (ex: text-primary-foreground/70).

NON disponibles (présents seulement dans legal-advisor-juris-gps — NE PAS utiliser) :
- `bg-brand` → utiliser `bg-primary`
- `text-gold` / `ring-gold` → utiliser `text-primary-foreground` (ou /60 pour atténuer)
- classe utilitaire `font-serif` → N'EXISTE PAS comme utilitaire Tailwind ici.
  Le serif s'applique via l'utilitaire CSS custom `hl-serif` (déjà utilisé dans landing.tsx, défini @utility dans globals.css).
- `motion/react` → NON installé. Panneau statique uniquement.
- `RouteMap` / illustrations de l'autre projet → NON disponibles ici.

`font-mono` EST disponible (utilisé dans landing.tsx pour les kickers).

Logo Esneo : `import { EsneoFullLogo } from '@/components/esneo-full-logo';`
  Signature : `EsneoFullLogo({ className, title='Esneo', ...props }: ComponentProps<'svg'> & { title?: string })`
  La couleur suit `text-foreground` par défaut ; pour le panneau bg-primary, forcer via className `text-primary-foreground`.

Clerk (déjà dans le projet) :
  `import { SignedIn, SignedOut, SignIn, SignUp } from '@clerk/nextjs';`
  `<SignedIn>` / `<SignedOut>` sont compatibles React Server Components (rendu via auth serveur) → landing.tsx PEUT rester Server Component.
  `<SignIn appearance={...} />` / `<SignUp appearance={...} />` acceptent un objet appearance { elements: {...} }.

Référence (autre projet, à ADAPTER, ne pas copier les tokens) :
  /Users/mrasoahaingo/Projects/perso/legal-advisor-juris-gps/components/auth/auth-screen.tsx
  /Users/mrasoahaingo/Projects/perso/legal-advisor-juris-gps/components/auth/auth-brand-panel.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liens conditionnels dashboard sur la landing (SignedIn/SignedOut)</name>
  <files>components/marketing/landing.tsx</files>
  <action>
    Ajouter `import { SignedIn, SignedOut } from '@clerk/nextjs';` en haut de landing.tsx.
    NE PAS ajouter `'use client'` — le composant reste un Server Component (SignedIn/SignedOut sont RSC-compatibles ; ThemeToggle reste un composant client importé séparément, inchangé).

    Remplacer les 4 emplacements de liens auth par des variantes conditionnelles. Garder EXACTEMENT les mêmes classes Button/Link existantes pour la cohérence visuelle ; n'envelopper que le contenu auth.

    1) SiteNav (le bloc `<div className="flex flex-1 items-center justify-end gap-2">` ~lignes 147-165) :
       - Garder `<ThemeToggle />` HORS des wrappers (toujours visible).
       - `<SignedOut>` : conserver tels quels les deux boutons "Se connecter" (ghost, /sign-in) et "Démo" (/sign-up, avec ArrowRight).
       - `<SignedIn>` : un seul Button (mêmes classes que le bouton "Démo" : `h-9 rounded-full px-4 text-sm font-semibold`, nativeButton={false}, render={<Link href="/dashboard" />}) avec le label "Mon dashboard" + `<ArrowRight className="size-4" />`.

    2) Hero CTA (le bloc `<div className="hl-reveal mt-9 ...">` ~lignes 191-206) :
       - `<SignedOut>` : conserver le Button "Réserver une démo" (/sign-up) ET le lien ancre "Voir comment ça marche" (#capacites).
       - `<SignedIn>` : Button "Accéder au dashboard" (mêmes classes que "Réserver une démo" : `h-12 rounded-full px-7 text-[0.95rem] font-semibold`, render={<Link href="/dashboard" />}, ArrowRight) + conserver le lien ancre "Voir comment ça marche".
       Astuce : pour ne pas dupliquer le lien ancre, garder le lien ancre en dehors des wrappers et ne wrapper que le Button.

    3) FinalCta (~lignes 435-442) :
       - `<SignedOut>` : conserver le Button "Réserver une démo" (/sign-up).
       - `<SignedIn>` : Button "Accéder au dashboard" (mêmes classes `h-12 shrink-0 rounded-full px-7 text-[0.95rem] font-semibold`, render={<Link href="/dashboard" />}, ArrowRight).

    4) SiteFooter nav (~lignes 459-464) :
       - Garder les ancres "Capacités" et "Sécurité" inchangées (hors wrappers).
       - `<SignedOut>` : conserver les deux liens "Se connecter" (/sign-in) et "Créer un compte" (/sign-up), mêmes classes.
       - `<SignedIn>` : un seul Link "Mon dashboard" (/dashboard) avec les mêmes classes `text-muted-foreground transition-colors hover:text-foreground`.

    Ne pas toucher aux autres sections de la page.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "landing.tsx" || echo "OK: no TS error in landing.tsx"</automated>
  </verify>
  <done>landing.tsx importe SignedIn/SignedOut, reste Server Component (pas de 'use client'), et les 4 emplacements (SiteNav, Hero, FinalCta, SiteFooter) affichent des liens /dashboard sous SignedIn et les liens auth d'origine sous SignedOut. tsc passe.</done>
</task>

<task type="auto">
  <name>Task 2: Composants auth Esneo — AuthBrandPanel (statique) + AuthScreen (2 colonnes)</name>
  <files>components/auth/auth-brand-panel.tsx, components/auth/auth-screen.tsx</files>
  <action>
    Créer le dossier components/auth/ avec deux fichiers ADAPTÉS depuis la référence legal-advisor, mais SANS motion/react, SANS RouteMap, et en n'utilisant QUE les tokens Esneo vérifiés.

    A) components/auth/auth-brand-panel.tsx (Server Component, PAS de 'use client') :
       - `<aside className="bg-primary text-primary-foreground relative hidden flex-col justify-between overflow-hidden p-10 md:flex lg:p-14">`.
       - En haut : lien retour `<Link href="/">` avec `<ArrowLeft className="size-4" />` (lucide-react) + le logo : `<EsneoFullLogo className="h-5 w-auto text-primary-foreground" title="Esneo" />`. Classe du lien : `group text-primary-foreground/70 hover:text-primary-foreground inline-flex w-fit items-center gap-2 text-sm transition-colors outline-none focus-visible:ring-2 focus-visible:ring-ring`.
       - Bloc central `max-w-md` :
         - kicker : `<p className="mb-5 font-mono text-[11px] uppercase tracking-[0.18em] text-primary-foreground/60">{kicker}</p>`
         - titre : `<h1 className="hl-serif text-[clamp(2rem,3.5vw,2.75rem)] leading-[1.06] font-semibold tracking-tight text-balance">{title} <span className="text-primary-foreground/60">{accent}</span></h1>` (utiliser hl-serif, PAS font-serif ; accent en /60 puisque pas de gold).
         - lede : `<p className="mt-5 text-base leading-relaxed text-pretty text-primary-foreground/75">{lede}</p>`
         - NE PAS inclure de RouteMap ni d'illustration.
       - Bas : une ligne de réassurance Esneo en `text-primary-foreground/55 max-w-md text-xs leading-relaxed text-pretty`, ex : "Chaque ESN dispose d'un espace isolé, avec une relecture humaine par défaut. Vos données restent privées."
       - Props : `{ kicker: string; title: string; accent: string; lede: string }`.

    B) components/auth/auth-screen.tsx (Server Component) :
       - Exporter `borderlessAppearance` (objet) pour fondre le formulaire Clerk dans le fond :
         `export const borderlessAppearance = { elements: { rootBox: 'w-full', cardBox: 'w-full border-none shadow-none', card: 'bg-transparent shadow-none border-none px-0', footer: 'bg-transparent' } };`
       - Exporter `AuthScreen({ kicker, title, accent, lede, children })` :
         `<div className="grid min-h-dvh grid-cols-1 md:grid-cols-2">`
           `<AuthBrandPanel kicker={kicker} title={title} accent={accent} lede={lede} />`
           `<main className="bg-background flex flex-col px-6 py-8 md:px-10 md:py-12">`
             retour mobile (visible < md uniquement) : `<Link href="/" className="group text-muted-foreground hover:text-foreground inline-flex w-fit items-center gap-2 text-sm transition-colors outline-none focus-visible:ring-2 focus-visible:ring-ring md:hidden">` avec `<ArrowLeft className="size-4" />` + `<EsneoFullLogo className="h-5 w-auto" title="Esneo" />`.
             `<div className="flex flex-1 items-center justify-center">{children}</div>`
           `</main>`
         `</div>`
       - Importer AuthBrandPanel, Link (next/link), ArrowLeft (lucide-react), EsneoFullLogo.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "components/auth/" || echo "OK: no TS error in components/auth"</automated>
  </verify>
  <done>components/auth/auth-brand-panel.tsx et auth-screen.tsx existent, n'importent ni motion/react ni RouteMap, utilisent EsneoFullLogo + hl-serif + tokens bg-primary/text-primary-foreground, et exportent AuthScreen + borderlessAppearance. tsc passe.</done>
</task>

<task type="auto">
  <name>Task 3: Brancher AuthScreen sur les pages sign-in et sign-up</name>
  <files>app/sign-in/[[...sign-in]]/page.tsx, app/sign-up/[[...sign-up]]/page.tsx</files>
  <action>
    Réécrire les deux pages pour utiliser AuthScreen avec des textes adaptés au contexte ESN (gestion CV consultants, analyse de missions, positionnement assisté par IA). Ne plus utiliser AuthBrandLogo (remplacé par le panneau/logo de AuthScreen).

    app/sign-in/[[...sign-in]]/page.tsx :
      ```tsx
      import { SignIn } from '@clerk/nextjs';
      import { AuthScreen, borderlessAppearance } from '@/components/auth/auth-screen';

      export default function SignInPage() {
        return (
          <AuthScreen
            kicker="Plateforme IA pour les ESN"
            title="Reprenez le fil de vos"
            accent="positionnements."
            lede="Connectez-vous pour retrouver vos CVs de consultants, vos analyses de missions et vos positionnements assistés par l'IA."
          >
            <SignIn appearance={borderlessAppearance} />
          </AuthScreen>
        );
      }
      ```

    app/sign-up/[[...sign-up]]/page.tsx : même structure avec SignUp et des textes orientés découverte :
      kicker="Plateforme IA pour les ESN"
      title="Du CV au positionnement,"
      accent="piloté par l'IA."
      lede="Créez votre espace ESN : extraction des CVs, analyse des missions et matching expliqué, avec une relecture humaine par défaut."
      enfant : `<SignUp appearance={borderlessAppearance} />`

    Ne pas modifier app/sign-in/layout.tsx (passe-plat suffisant). Conserver les segments de route [[...sign-in]] / [[...sign-up]] tels quels.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sign-in|sign-up" || echo "OK: no TS error in auth pages"</automated>
  </verify>
  <done>Les deux pages importent AuthScreen + borderlessAppearance, passent SignIn/SignUp en enfant avec appearance, et utilisent des textes kicker/title/accent/lede ESN. N'importent plus AuthBrandLogo. tsc passe.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne remonte aucune erreur sur les fichiers touchés.
- Build/lint : `npx next lint` ne signale pas d'erreur sur landing.tsx, components/auth/*, pages auth.
- Vérification visuelle manuelle recommandée (hors automatisation) : déconnecté → liens auth ; connecté → liens /dashboard ; /sign-in et /sign-up en 2 colonnes desktop, mono-colonne + logo en mobile.
</verification>

<success_criteria>
- Landing : SignedOut affiche les liens auth d'origine ; SignedIn affiche des liens /dashboard, aux 4 emplacements (nav, hero, final CTA, footer).
- landing.tsx reste Server Component (pas de 'use client' ajouté).
- components/auth/auth-screen.tsx + auth-brand-panel.tsx créés, sans motion/react ni RouteMap, tokens Esneo uniquement (bg-primary/text-primary-foreground/hl-serif/font-mono), logo EsneoFullLogo.
- /sign-in et /sign-up rendent AuthScreen en 2 colonnes avec formulaire Clerk borderless.
- Aucune erreur TypeScript.
</success_criteria>

<output>
After completion, create `.planning/quick/260701-1wi-landing-liens-dashboard-si-connect-pages/260701-1wi-SUMMARY.md`
</output>
