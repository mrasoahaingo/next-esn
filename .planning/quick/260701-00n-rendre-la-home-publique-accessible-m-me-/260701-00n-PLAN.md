---
phase: quick-260701-00n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/authenticated-shell.tsx
autonomous: true
requirements: [HOME-PUBLIC]
must_haves:
  truths:
    - "A logged-out user visiting / sees the marketing landing (already works)"
    - "A logged-in user visiting / sees the marketing landing instead of being redirected to /dashboard"
    - "The landing renders bare (no sidebar/header shell) for / regardless of auth state"
    - "page.tsx metadata export is preserved"
  artifacts:
    - path: "app/page.tsx"
      provides: "Public home rendering <Landing /> without auth redirect"
      contains: "return <Landing />"
    - path: "components/authenticated-shell.tsx"
      provides: "Bare render for pathname === '/' before the logged-in shell branch"
      contains: "pathname === '/'"
  key_links:
    - from: "components/authenticated-shell.tsx"
      to: "children (Landing)"
      via: "early return <>{children}</> when pathname === '/'"
      pattern: "pathname === '/'"
---

<objective>
Make the home route `/` truly public: render the marketing `<Landing />` even when the user is authenticated, without the app shell.

Purpose: The home was converted into a public marketing landing, but two guards still block logged-in users from seeing it (a redirect in page.tsx and the app-shell wrap in authenticated-shell.tsx).
Output: `app/page.tsx` without the auth redirect, `components/authenticated-shell.tsx` rendering bare children for `/` regardless of auth state.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/page.tsx
@components/authenticated-shell.tsx

<interfaces>
Current app/page.tsx (lines 26-35) redirects logged-in users:
```tsx
export default async function HomePage() {
  const { userId } = await auth();
  if (userId) {
    redirect('/dashboard');
  }
  return <Landing />;
}
```

Current components/authenticated-shell.tsx structure:
- Lines 31-45: `if (!userId)` branch — already returns `<>{children}</>` when `pathname === '/'` (line 34).
- Lines 47-58: logged-in branch — always wraps children in `OrgBrandingProvider > SidebarProvider > UnifiedSidebar + AppHeader`. This is the branch that must be bypassed for `/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the auth redirect from the home page</name>
  <files>app/page.tsx</files>
  <action>
    Make HomePage render `<Landing />` unconditionally. Remove the `auth()` call and the `if (userId) redirect('/dashboard')` block (current lines 27-32) so the body becomes a plain `return <Landing />`.

    The component no longer needs to be async since there is no awaited call — change `export default async function HomePage()` to `export default function HomePage()`.

    Clean up now-unused imports: remove `import { redirect } from 'next/navigation'` (line 1) and `import { auth } from '@clerk/nextjs/server'` (line 2). Keep `import { Landing } from '@/components/marketing/landing'`.

    MUST preserve the `TITLE`, `DESCRIPTION` constants and the `export const metadata = {...}` block unchanged.
  </action>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "app/page.tsx" || echo "no page.tsx type errors"</automated>
  </verify>
  <done>app/page.tsx returns `<Landing />` with no redirect/auth logic, no unused imports, metadata export intact, and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Render the landing bare for / when authenticated</name>
  <files>components/authenticated-shell.tsx</files>
  <action>
    In the logged-in code path (the `userId` is truthy fall-through after the `if (!userId)` block, currently starting at line 47), add an early return for the public landing BEFORE the `OrgBrandingProvider`/`SidebarProvider` shell wrap:

    ```tsx
    // The home route is the public marketing landing — render it bare even
    // when the user is authenticated (no sidebar/header shell).
    if (pathname === '/') {
      return <>{children}</>;
    }
    ```

    Place this check immediately after the `if (!userId) { ... }` block and before the `return ( <OrgBrandingProvider> ... )`. Do NOT modify the existing logged-out `/` branch (lines 34-35) or the `isPublicPath` helper.
  </action>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "authenticated-shell" || echo "no authenticated-shell type errors"</automated>
  </verify>
  <done>For pathname `/`, AuthenticatedShell returns bare `<>{children}</>` in both the logged-out and logged-in branches; all other authenticated routes still get the full shell.</done>
</task>

</tasks>

<verification>
- `app/page.tsx` renders `<Landing />` with no auth/redirect imports; metadata preserved.
- `components/authenticated-shell.tsx` short-circuits to bare children for `/` regardless of auth state.
- `npx tsc --noEmit` reports no new type errors in the touched files.
- Manual smoke (optional): logged-in user navigating to `/` sees the landing, not the dashboard or the app shell.
</verification>

<success_criteria>
- Logged-in user at `/` sees `<Landing />` (no redirect to `/dashboard`).
- Landing renders without sidebar/header shell for `/` in both auth states.
- No unused imports; metadata export intact; typecheck clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260701-00n-rendre-la-home-publique-accessible-m-me-/260701-00n-SUMMARY.md`
</output>
