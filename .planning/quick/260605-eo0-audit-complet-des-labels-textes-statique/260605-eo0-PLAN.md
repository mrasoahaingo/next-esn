---
phase: quick-260605-eo0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/cv-dossier-layout.ts
autonomous: true
requirements:
  - LABELS-AUDIT-01
must_haves:
  truths:
    - "All static labels in the CV PDF template are translated when language='en'"
    - "The 'Present' end-date fallback is language-aware (Présent in FR, Present in EN)"
    - "No hardcoded French text remains in the template layout builder"
  artifacts:
    - path: "templates/cv-dossier-layout.ts"
      provides: "CV_LABELS with full fr/en coverage including 'present' key"
      contains: "present: 'Présent'"
  key_links:
    - from: "CV_LABELS[lang].present"
      to: "exp.endDate ?? L.present"
      via: "addExperiencesBlock dateText computation"
      pattern: "L\\.present"
---

<objective>
Audit and fix all static labels in the CV PDF template to ensure complete English translation when language='en'.

Purpose: A French CV shows "Présent" as current job end date and French section labels. An English CV must show "Present" and English labels throughout. The audit confirmed CV_LABELS is correctly wired to all block builders — the only gap is the missing `present` label for the `endDate` fallback in experiences.

Output: Updated `templates/cv-dossier-layout.ts` with `present` added to CV_LABELS (fr+en) and used in `addExperiencesBlock`.
</objective>

<execution_context>
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mrasoahaingo/Projects/perso/next-esn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

## Audit findings (pre-computed — no codebase exploration needed)

The audit of `templates/cv-dossier-layout.ts` revealed:

**CV_LABELS coverage — COMPLETE:**
- Section headings: summaryHeading, skillsHeading, educationHeading, experiencesHeading — all wired
- Info table labels: poste, yearsOfExperience, location, availability, email, phone — all wired
- Skills sub-categories: technologies, softSkills, expertises, methodologies — all wired
- Document title: docTitle — wired
- All block builders use `const lang = data.language ?? 'fr'; const L = CV_LABELS[lang];`

**Only gap found:**
- Line 551: `const dateText = \`${exp.startDate ?? ''} - ${exp.endDate ?? 'Present'}\``
- `'Present'` is hardcoded in English — French CVs show "Present" instead of "Présent"
- `isCurrent` field exists on the experience schema but is not used in date rendering
- Fix: add `present` key to CV_LABELS (`fr: 'Présent'`, `en: 'Present'`), use `L.present` as fallback

**No other hardcoded French text found** in the template builder.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add present label to CV_LABELS and fix dateText in experiences</name>
  <files>templates/cv-dossier-layout.ts</files>
  <action>
    Two changes to `templates/cv-dossier-layout.ts`:

    **1. Add `present` to the CV_LABELS type and both locale objects:**

    In the `CV_LABELS` Record type definition (around line 22), add `present: string` to the type shape.

    In the `fr` locale object, add: `present: 'Présent',`
    In the `en` locale object, add: `present: 'Present',`

    Place after the `methodologies` key in both locales (last key in each object).

    **2. Fix dateText in addExperiencesBlock (around line 551):**

    Current:
    ```typescript
    const dateText = `${exp.startDate ?? ''} - ${exp.endDate ?? 'Present'}`;
    ```

    Replace with:
    ```typescript
    const endLabel = exp.isCurrent ? L.present : (exp.endDate ?? L.present);
    const dateText = `${exp.startDate ?? ''} - ${endLabel}`;
    ```

    `L` is already in scope at that point (`const L = CV_LABELS[lang]` on line 544).
  </action>
  <verify>
    <automated>cd /Users/mrasoahaingo/Projects/perso/next-esn && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - CV_LABELS has `present` key in fr ('Présent') and en ('Present') locales
    - dateText in addExperiencesBlock uses `L.present` — no hardcoded English string remains
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After the fix, verify by grep:
```bash
grep -n "Present\|Présent" /Users/mrasoahaingo/Projects/perso/next-esn/templates/cv-dossier-layout.ts
```
Expected: Only `present: 'Présent'` and `present: 'Present'` in CV_LABELS, and `L.present` in dateText. No bare `'Present'` string outside CV_LABELS.
</verification>

<success_criteria>
- `CV_LABELS.fr.present === 'Présent'`
- `CV_LABELS.en.present === 'Present'`
- Line 551 no longer contains `?? 'Present'` bare string
- TypeScript compiles without errors
- French CV shows "Présent" for current jobs, English CV shows "Present"
</success_criteria>

<output>
After completion, create `.planning/quick/260605-eo0-audit-complet-des-labels-textes-statique/260605-eo0-SUMMARY.md`
</output>
